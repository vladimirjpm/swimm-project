// Единый контейнер ширины страницы (design_handoff_competition_overviewv2, блок 5a).
// Полосы-фоны (topbar / hero соревнования / табы) идут КРАЙ-В-КРАЙ, а их содержимое
// и контент страницы ограничиваются этим контейнером — иначе на 1920 шапка и Overview
// оказывались шире таблиц результатов. Локальных lg:max-w-[1180px] у таблиц больше нет.
export const PAGE_CONTAINER = 'mx-auto w-full max-w-[1440px] px-4 lg:px-6';

/**
 * Колонки КОНТЕНТА внутри страницы — классы `content-box-xs | -sm | -md | -lg | -xl`
 * (780 / 850 / 920 / 1050 / 1180). Живут не здесь, а в `src/index.css`: это утилиты
 * Tailwind, их надо уметь писать строкой в любом `className`, а не только импортом.
 * Там же — почему лесенка и почему без медиазапросов.
 *
 * `PAGE_CONTAINER` выше и они — РАЗНОЕ: он держит ширину страницы и боковые паддинги,
 * они вкладываются внутрь него и задают только ширину колонки.
 */
