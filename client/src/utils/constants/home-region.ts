/**
 * «Домашний» регион приложения — единственная точка, где клиент знает, чьи
 * национальные рекорды запрашивать и как подписывать колонки/карточки NR.
 * Код — alpha-3 World Aquatics, как везде в данных (см. docs/ARCHITECTURE.md,
 * решение 2026-07-13). Смена страны деплоя = правка этих двух констант
 * (+ RecordsImport:WorldAquaticsNationalCountryId на сервере).
 */
export const HOME_REGION = 'ISR';
export const HOME_REGION_LABEL = 'Israel';

/**
 * Система нормативов (разрядов), которую показывает UI — НЕ то же самое, что HOME_REGION
 * (тот — чьи национальные рекорды). Сейчас в БД только российская система ("RUS"), но
 * NormativeStandard.Country уже поддерживает несколько стран (обещан израильский набор) —
 * без этой константы клиент запрашивал бы все страны разом и деревья бы перезаписывали
 * друг друга. Это константа, не настройка пользователя (переключатель — отдельная фаза).
 */
export const NORMATIVE_COUNTRY = 'RUS';
