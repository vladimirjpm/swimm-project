using Xunit;

namespace Swimm.Tests;

/// <summary>
/// Тесты, которые ПИШУТ в живую локальную базу (заводят свои строки и удаляют их в finally), и
/// тесты, которые собирают из неё страницы целиком и сверяют их, — в одной коллекции xUnit: внутри
/// неё классы идут по очереди, остальные тесты — параллельно, как раньше.
///
/// Иначе чужие временные строки видны в окне между созданием и удалением: страницы на блоках
/// сужения, собранные из «всех групп базы», ловили временную группу <c>pg-club-sub-…</c> и падали
/// с «группа пропала из базы» (К4б.5, 14.09.2026), а сверка состава клуба могла бы разойтись из-за
/// временного пловца. Новый PG-тест, который пишет в живые таблицы, — сюда же.
/// </summary>
[CollectionDefinition(Name)]
public sealed class LiveDbCollection
{
    public const string Name = "Живая база";
}
