# Local SQL Server files
/data/
*.mdf
*.ldf
*.ndf

# Локальные настройки разработки с секретами (Google ClientSecret и т.п.) — не коммитить
Swimm.API/appsettings.Development.json
# Локальное переключение БД (например, временное подключение к проду) — не коммитить
Swimm.API/appsettings.Local.json
