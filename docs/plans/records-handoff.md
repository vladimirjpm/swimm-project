# Рекорды: передача в новый чат (16.09.2026, после #39/#40/#42)

Короткая точка входа. Подробности — в планах:
[records-all-countries-plan.md](records-all-countries-plan.md) (Фаза 11: этапы, §3а — разведка
источника, §8 — порядок работ) и [records-quality-plan.md](records-quality-plan.md) (реестр
спорных записей, §3-бис — сторож импорта). Инциденты и решения по данным —
[../data-integrity.md](../data-integrity.md): И-13 (порядок источников), И-19 (`=WR`), И-20
(невозможное время), раздел про `--records-refresh`.

## Где мы

- **Всё сделанное — в `master`, открытых PR по рекордам нет, CI зелёный.** #39 (`=WR`,
  merge `6f46297`), #40 (сторож правдоподобия, `f5a57c6`), #42 (запись в журнал, `3a45f3c`).
- **#39:** повторённый мировой рекорд (`=WR` в отчёте World Aquatics) больше не уезжает в
  национальные. Миграция `DropEqualledWorldRecordStrays` убрала одиночки `country/USA/open` и
  `country/JAM/open` — только пока у страны одна строка (после Фазы 11 это законные NR).
- **#40:** дифф рекордов проверяет правдоподобие времени и заводит находки в реестр спорных
  **кандидатами**. Решение Влада 15.09: не блокировать, а метить.
- **Данные после боевого прогона 15.09** (локальная БД): `world` 86, `country/ISR` — open 90,
  age 781, masters 726. В реестре две записи, обе `open`: RQ-1 (34.08) и 40.11.
  Мастерские мировые (`world/masters`, источник `wa-masters`) — источник сделан 16.09,
  1095 строк в dry-run, **в базу не заливались**.
- **Шаг 1 (11.1.1) сделан 16.09**, ветка `records-country-list`: провайдер списка стран
  (`WorldAquaticsCountriesProvider`, шов `IRecordCountriesProvider`), общий
  `WorldAquaticsSource` вместо копии предиката SSRF, 6 тестов. Живой прогон: 235 стран.
- **Шаг 2 (11.1.3 п. 2) сделан 16.09**, ветка `records-country-fetch` поверх шага 1:
  `WorldAquaticsCountryFetcher` (шов `IRecordCountryFetcher`) — NR одной страны по GUID,
  `RegionCode` из запроса, непринятые строки в `RecordCountryMismatchDto`. Живой прогон:
  Ангилья 18 рекордов, 0 непринятых.
- **Шаг 3 (11.1.2) сделан 16.09**, ветка `records-country-run` поверх шага 2: фоновый прогон
  по странам (очередь, прогресс, упавшие, дифф на 12 ч), «нет в источнике» сужено до регионов
  прогона, эндпоинты `/api/admin/records/countries[/run]`. Живой прогон AGU + JAM: 175 строк,
  дифф только `open` этих стран. **Apply не делался** — заливка это шаг 6.
- **Шаг 4 (11.1.5 + 11.1.4-дашборд) сделан 16.09**, ветка `records-scale-neighbours`: сверка
  качества и её сводка считаются только по `world` + `ISR`, блок дашборда сворачивает чужие
  страны в одну строку. Осталось глазами после заливки: страница пловца ищет рекорды по имени
  держателя во всех регионах (иностранцам из наших протоколов добавятся рекорды их стран).
- Тесты: 1997/1997 при слиянии #42, 2011 после шага 1, 2019 после шага 2, 2028 после шага 3,
  2030 после шага 4.

## Как устроено (в двух словах)

- **Источники:** `worldrecords` (4 XLSX World Aquatics: WR SCM/LCM + NR SCM/LCM **одной**
  страны, GUID в `RecordsImport:WorldAquaticsNationalCountryId`), `isrorg-age` и
  `isrorg-masters` (PDF федерации, ссылки резолвятся со страницы-оглавления) и `wa-masters`
  (PDF мастерских МИРОВЫХ рекордов с worldaquatics.com, тоже через страницу-оглавление).
- **Путь:** дифф по 8 осям (`RecordDiffService`) → превью 10 мин → Apply. Apply **только
  добавляет и обновляет**, удалений нет; метки кэша запись получает сама (К4).
- **Порядок источников — само правило** (И-13): сперва World Aquatics, потом федерация поверх.
  Зашит списком в `--records-refresh`; перепутаешь — молча откатятся израильские рекорды.
- **Сторож** (`RecordPlausibility`): мировой улучшен за раз > 3 % или рекорд страны / возраста
  быстрее мирового → строка идёт в `RecordDiffResult.Suspicious`, при Apply заводится
  `Sys_RecordIssues` со статусом `candidate`. На витрине метки нет, пока человек не переведёт
  запись в `open` — «автомат только предлагает» (§3 плана качества).

## Что дальше — Фаза 11.1, порядок из §8 плана

1. ✅ 16.09.2026 · Список стран из `/fina/countries` — `WorldAquaticsCountriesProvider`,
   235 реальных стран из 246 записей ответа (11.1.1).
2. ✅ 16.09.2026 · Провайдер «NR одной страны по GUID» — `WorldAquaticsCountryFetcher`:
   код региона из запроса, таймаут 120 с, два повтора, отчёт о непринятых строках (11.1.3 п. 2).
3. ✅ 16.09.2026 · Фоновая задача — очередь `RecordCountryRunQueue` + `RecordCountryRunner`:
   WR один раз, NR по странам, пауза 2 с, дифф живёт 12 ч, **ISR не входит никогда** (11.1.2).
4. ✅ 16.09.2026 · Сверка качества сужена до `world` + `ISR` (`RecordQualityService.Verifiable`),
   блок «Рекорды» на дашборде свёрнут в «мир · Израиль · Другие страны · стран: N» (11.1.5, 11.1.4).
5. Вкладка «Рекорды» на `/Admin/Import`: выбор стран, прогресс, дифф по странам.
6. Боевой прогон: сначала 3–5 стран, потом все.
7. ✅ 16.09.2026 · Источник `wa-masters` — мастерские МИРОВЫЕ рекорды (11.1.6): резолвер
   страницы worldaquatics.com/masters/records, парсер PDF, карточка на `/Admin/Import`,
   место в `--records-refresh` — сразу за `worldrecords`, до федеральных источников.
   **Apply не делался** — `world/masters` в базе пока пусто.
   Подробности — `docs/admin-pages/import.md` и раздел «Мастерские мировые рекорды»
   в `docs/data-integrity.md`.

**Решено Владом 16.09.2026 (§7 плана закрыт, все четыре по рекомендациям):** тянем **все 235
реальных стран**; `RegionCode` берём **из запроса**, несовпадения — строкой в отчёт прогона;
`TimeMs` — **колонка миграцией**, делается в 11.2 вместе с рейтингом, для 11.1 не нужна;
`ISR` в батче **пропускаем** (Израиль только через `--records-refresh`, порядок И-13).

## Как проверять вживую

```bash
# пробный прогон (ходит в сеть; строки ⚠ — находки сторожа)
dotnet run --project server/Swimm.API -- --records-refresh --dry-run

# тесты рекордов
dotnet test server/Swimm.Tests --filter "FullyQualifiedName~Record"

# мастерские мировые на РЕАЛЬНЫХ PDF (иначе тесты скипаются): положить в папку
# CurrentWorldRecords-Individual-LCM.pdf и …-SCM.pdf со страницы worldaquatics.com/masters/records
SWIMM_WA_MASTERS_DIR=<папка> dotnet test server/Swimm.Tests --filter "FullyQualifiedName~WaMasters"

# на РЕАЛЬНЫХ отчётах: положить WR_SCM/WR_LCM/NR_SCM/NR_LCM.xlsx в папку и задать
SWIMM_WR_REPORTS_DIR=<папка> dotnet test server/Swimm.Tests --filter "FullyQualifiedName~WorldRecords"

# метка на витрине: у 40.11 должно быть issue_reason
curl -s "http://localhost:5078/api/records?region=world&category=open"
```

## Грабли

- **Прода нет** (проверено `az` 15.09): «прод» — это будущий дамп локальной БД, `Records` и
  `Sys_RecordIssues` в `server/db/seed-tables.txt` входят. Не писать «применить на проде».
- **Прогон идёт отдельным процессом** и кэш запущенного API не сбрасывает: после
  `--records-refresh` — общий сброс на `/Admin/Cache` или перезапуск, иначе страницы с рекордами
  до суток показывают прежнее.
- **API World Aquatics медленный:** 2–53 с на файл (замер 15.09), у провайдера таймаут 30 с —
  для батча поднимать до 120 с и добавлять повторы. Полный прогон 470 файлов — час-два.
- **Колонка `Country` в отчёте — страна МЕСТА соревнования**, не спортсмена (тот в `NF Code`).
  Фоллбек на неё в режиме «все страны» запрещён — иначе регионы вида `Great Britain`.
- **Удаление «нет в источнике» не добавлять:** у малых федераций наборы неполные, а у
  `country/ISR/open` есть строки, которых World Aquatics не отдаёт.
- **Реестр:** ключ — 8 осей + время, поэтому повторный импорт не воскрешает снятую (`rejected`)
  претензию и не плодит дубли. Метка — значок качества у времени; расчёты (дельта до мирового)
  берут значение как есть.
