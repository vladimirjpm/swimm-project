# План — Фаза 11: рекорды всех стран, рейтинг, head-to-head

Рабочий план к [Фазе 11 роадмапа](../ROADMAP.md#фаза-11--рекорды-всех-стран-импорт-рейтинг-head-to-head).
История импорта рекордов (этап 2.6, что уже работает) — в
[ROADMAP §2.6](../ROADMAP.md#фаза-2--рекорды-и-нормативы-в-бд).

Составлен 2026-07-29, **дополнен 2026-09-15** разведкой источника (§3а): живой список стран,
замер скорости API, найдена причина мусорных `USA`/`JAM`, новая ловушка с Израилем (§4,
11.1.2), соседи, которых заденут 10k новых строк (11.1.5), порядок работ (§8).
Статусы: ☐ не начато · ◐ в работе · ✅ готово. **Этап 11.1 закрыт 16.09.2026** (импорт всех
стран пройден боевым прогоном); 11.2 и 11.3 не начаты.

---

## 1. Задача (со слов Влада)

1. В админке сейчас можно скачать рекорды **мира и Израиля** — нужна возможность скачивать
   рекорды **всех стран**.
2. Запланировать страницу **рейтинга рекордов** с фильтрами.
3. Запланировать страницу **head-to-head** — сравнение двух стран по рекордам.

**РЕШЕНО (Влад, 2026-07-29): для других стран только `Category='open'`** — национальные
рекорды. `age` и `masters` по СТРАНАМ остаются **исключительно израильскими**: их источник —
PDF isr.org.il, а у остальных стран таких справочников у World Aquatics нет. `junior` в этой
фазе не трогаем.

⚠ **Уточнение 16.09.2026.** Про мировую ось это было неверно: мастерские МИРОВЫЕ рекорды
World Aquatics ведёт и публикует — просто не в XLSX-отчёте (`recordCode` там принимает только
`WR`/`NR`), а PDF-ками на worldaquatics.com/masters/records. Решением Влада заведён источник
`wa-masters` (`world/masters`, 1095 строк, только личные дистанции) — шаг 11.1.6 в §8 и раздел
«Мастерские мировые рекорды» в `docs/data-integrity.md`. Решение «страны — только `open`» это
не меняет.

## 2. Что есть сейчас

**Данные** (замер локальной БД **2026-09-16, после боевого прогона по странам и чистки И-24**;
было → стало, в скобках — 2026-07-29). Всего **16 685** строк:

| RegionType | RegionCode | Category | строк |
|---|---|---|---|
| country | ISR | age | 781 (779) |
| country | ISR | masters | 726 → **845** (16.09: приехала короткая вода, И-21) |
| country | ISR | open | 90 (105) |
| world | (пусто) | open | 86 (101) |
| world | (пусто) | masters | **1095** — источник `wa-masters`, залито 16.09.2026 |
| country | прочие 211 стран | open | 0 → **13 788** — боевой прогон 16.09 (шаг 6) |

`open`-строк стало меньше из-за чистки фантомного стиля `medley` 24.08 (32 строки, у каждой был
канонический близнец — `docs/data-integrity.md`). Таблица `Countries` — уже **100 строк**
(в июле была одна).

**Схема** `Record` — три независимые оси, к мультистрановости готова:
`RegionType` (world/continent/country) + `RegionCode` · `Category` + `AgeKey` ·
`Gender` + `PoolType` + `Style` + `Distance`; плюс `Time`, `HolderName`, `Club`,
`HolderCountry`, `RecordDate`, `UpdatedAt`.

**Импорт** (этап 2.6, работает): `IRecordSourceProvider` × 4 —
`WorldRecordsSourceProvider` (4 XLSX с api.worldaquatics.com: WR SCM/LCM + NR SCM/LCM),
`IsrOrgAgeRecords`, `IsrOrgMastersRecords` (PDF федерации) и `WaMastersRecordsSourceProvider`
(PDF мастерских мировых, 11.1.6). Дифф по 8 осям
(`RecordDiffService` + `DeduplicateByAxes`), превью-сессия 10 мин, Apply в транзакции +
инвалидация кэша. Эндпоинты `/api/admin/records/fetch|apply|source-status`, UI — вкладка
«Рекорды» на Admin/Import карточками-источниками.

С 2026-08-24 есть CLI `--records-refresh` — прогон `worldrecords` → `wa-masters` →
`isrorg-age` → `isrorg-masters` в жёстком порядке (И-13, см. 11.1.2; `wa-masters` и
`isrorg-masters` дописаны 2026-09-16 — до того мастерсов в прогоне не было вовсе). **Apply только добавляет и обновляет, удалений в нём нет**:
«нет в источнике» — лишь счётчик в превью (`RecordDiffService.ApplyAsync`).

**Эта фаза разблокирует 9.9** (переключатель региона рекордов), который стоял
«заблокирован данными».

---

## 3. Пять технических фактов, определяющих объём

1. **Качается NR ровно одной страны.** GUID страны — в конфиге
   `RecordsImport:WorldAquaticsNationalCountryId` (дефолт Израиль
   `962f77d6-…`). В проекте справочника «GUID worldaquatics ↔ alpha-3» нет, **но его отдаёт
   сам API** — см. §3а.
2. **`User-Agent` обязателен** — без него api.worldaquatics.com не отвечает вовсе (висит до
   таймаута). Выстрадано на приёмке 2.6.
3. **`RegionCode` берётся не из запроса**, а из кода страны спортсмена в строке XLSX
   (NR/CR → код страны). *Уточнено 2026-09-15 (§3а):* в NR-файлах этот код совпадает с
   запрошенной страной, а мусорные `USA`/`JAM` — это повторённые мировые рекорды `=WR`.
4. **`Record.Time` — СТРОКА** («21.08», «01:43.45»). Рейтинг сортирует по времени →
   строковая сортировка не годится, нужен парсинг в миллисекунды.
5. **`RecordDate` — строка со смешанными форматами** (обычно `dd/MM/yyyy`, в легаси
   встречается `M/d/yyyy`). Для показа и сортировки по дате нужна нормализация.

Плюс: `/api/records` **требует** `region` (400 без него) — рейтинг «поверх всех стран» через
него не сделать. Легаси-дерево `window.normative_*` / `records-helper.ts` с ключом `ISR→NR`
рассчитано на одну страну — новые страницы ходят в API напрямую, минуя дерево.

---

## 3а. Разведка источника (2026-09-15)

Скачано вживую: список стран, WR SCM/LCM и NR SCM/LCM трёх стран — `USA` (большая), `JAM`
(средняя), `AGU` (Ангилья, крошечная).

**Список стран отдаёт сам API.** `GET https://api.worldaquatics.com/fina/countries` (тоже с
`User-Agent`) — один запрос, 246 записей: `Id` (тот самый GUID для `countryId`), `Code`
(alpha-3, дублей нет), `Name`, `RegionName` (континент). GUID Израиля совпадает с конфигом.
У 11 записей `RegionName` пуст — это не страны: `AIN`, `CLB`, `EOR`, `FINA`, `IOA`, `IOC`,
`IOP`, `NAA`, `NAB`, `NAC`, `SMF` (нейтральные атлеты, сборная беженцев, «Club», сама FINA…).
**Реальных стран 235** → 470 NR-файлов + 2 WR на полный прогон.
⚠ **Уточнено после боевого прогона 16.09.2026 (И-24):** среди этих 235 — 20 записей, которые
страной не являются, но регион у них ЗАПОЛНЕН, поэтому отсев по пустому региону их не ловил:
16 расформированных государств и объединённых команд (ГДР, СССР, Чехословакия, Югославия,
Саар, Родезия, ОАР, Южный Вьетнам, Вест-Индия, Нидерландские Антилы…) и 4 нейтральные сборные
(ART, NIA, ROC, RSF). По решению Влада 16.09.2026 они убраны списком
`WorldAquaticsCountriesProvider.NotCurrentCountries`. **Действующих стран 215** → 430
NR-файлов + 2 WR.

**Колонки отчёта:** `Event, Pool, Record Description, Record, Time, Athlete, NF Code, Gender,
Competition, Country, City, Date, Splits, Tags, Status`.
- `NF Code` — федерация спортсмена. Во всех NR-файлах выборки (6 файлов, 181 строка) он равен
  запрошенной стране.
- ⚠ **`Country` — страна МЕСТА соревнования**, полным названием («Great Britain»), а не страна
  спортсмена. Парсер берёт её запасным вариантом, когда `NF Code` пуст
  (`WorldRecordsParser`, строка `string country = …`) — на массиве стран это прямая дорога к
  регионам вида `Great Britain`. В режиме «все страны» этот фоллбек использовать нельзя.
- `Record` бывает составным: `NR, WR`, `NR, AM`, `NR, CR`, `=WR`… Парсер понимает ровно `WR`,
  всё прочее считает национальным.
- `Date` — настоящая дата Excel (не строка), `Status` во всей выборке — `Approved`.

**Откуда `USA`/`JAM` — найдено.** В WR SCM три строки с типом `=WR` (повторённый мировой
рекорд): 100 на спине ж (USA, 54.02) и 100 брасс ж (JAM, 1:02.36, две строки).
`NormalizeRecordType` ждёт ровно `WR` → строка считается национальной, регион берётся из
`NF Code` → она уезжает в `country/USA/open` и `country/JAM/open`. Это баг **сегодняшнего**
импорта, а не только Фазы 11: соавтор мирового рекорда теряется из `world`.
**Починен 2026-09-15** (шаг 0, `docs/data-integrity.md` И-19).

**Мусор бывает и в самом источнике (И-20).** Пробный `--records-refresh --dry-run` 15.09
показал в отчёте WR LCM «мировой рекорд» 40.11 на 100 вольным ж (`Status = Approved`, но
`Record Description = Meet Record`). Импорт правдоподобие не проверяет — на 235 странах
такого будет больше. Отсюда п. 4 в 11.1.3.

**Скорость API.** 8 XLSX: от 2 до 53 с на файл (WR SCM — 52.7 с, NR USA SCM — 30.0 с,
остальные 2–16 с). У `WorldRecordsSourceProvider` таймаут 30 с — **2 файла из 8 в него не
уложились бы**. Если это не разовая медлительность, падает и нынешняя кнопка Fetch (контроллер
ловит `HttpRequestException`, а таймаут — `TaskCanceledException`, будет 500). Полный прогон
470 файлов с паузой 2 с — **от часа до двух**.

**Объём.** NR на страну, оба бассейна: USA 91, JAM 72, AGU 18 строк (до отсева микст-эстафет).
Оценка по всем странам — **≈10–15k строк**, меньше июльских 20k.

---

## 4. Этап 11.1 — импорт рекордов всех стран

### 11.1.1 Перечисление стран ✅ 2026-09-16
Сделано как и планировалось: список тянется живым в начале прогона, в БД не хранится.
`WorldAquaticsCountriesProvider` (шов `IRecordCountriesProvider` → `RecordCountryDto`)
делает один запрос `GET /fina/countries` и отдаёт **215 стран**, отсортированных по коду.
Заодно домен, URL и HTTP-клиент источника съехали в `WorldAquaticsSource` — один предикат
SSRF на все обращения к worldaquatics (у `WorldRecordsSourceProvider` была своя копия).

Отсев — по трём признакам, и каждый нужен: пустой `RegionName` (ровно у 11 псевдо-сборных
AIN, CLB, EOR, FINA, IOA, IOC, IOP, NAA, NAB, NAC, SMF — других отличий в ответе нет), код
не из трёх букв (`FINA` — четыре, а код едет в `Record.RegionCode`, где alpha-3 — контракт)
и `Id`, не разбирающийся как GUID (в URL отчёта пойдёт только распознанный GUID). Дубль кода
не проходит: ключ прогона обязан быть уникальным. Пустой результат — исключение, а не
«обновлено 0 стран».

Миграции нет, таблицу `Countries` импорт по-прежнему не трогает; названия стран для витрины —
вопрос этапа 11.2.
**Приёмка выполнена:** `WorldAquaticsCountriesProviderTests` — 5 тестов на зафиксированном
фрагменте живого ответа (2026-09-16) + живой под `SWIMM_NET_TESTS=1`; живой прогон 16.09
дал 235 стран, 5 регионов, GUID Израиля совпал с зашитым в конфиг. После отсева
расформированных и нейтральных (И-24, там же) живой замер даёт **215**, и это же число
проверяет `Live_Countries_Still215`.

### 11.1.2 Батч-фетч (только NR, `open`) ✅ 2026-09-16 (боевой прогон пройден, шаг 6 закрыт)
Сделано как задумано: `RecordCountryRunQueue` (очередь и статусы в памяти, singleton) +
`RecordCountryRunBackgroundService` (обвязка) + `RecordCountryRunner` (ход прогона —
отдельным классом, чтобы прогонять тестом без хоста). Эндпоинты:
`POST /api/admin/records/countries/run` (коды или пусто = все), `GET …/countries/run/{id}`
(прогресс), `GET …/countries` (список стран для выбора); применяется обычным Apply по
`DiffId` из статуса. Вкладка админки — этап 11.1.4.

- 215 стран × 2 бассейна = **430 NR-файлов**; WR SCM/LCM — **один раз на прогон**
  (`FetchWorldAsync`), не на страну. Мировые нужны не только ради свежего `world`: сторож
  правдоподобия сверяет с ними национальные, и на устаревшем эталоне «быстрее мирового»
  срабатывало бы там, где рекорд просто новее. WR не скачались — прогон идёт дальше, эталон
  берётся из базы.
- Пауза между странами 2 с (вежливость, как в Discovery), таймаут на файл 120 с и два
  повтора — в `WorldAquaticsCountryFetcher` (шаг 2).
- Прогресс: «страна N из M», текущая страна, набранные строки. Упавшая страна прогон не
  роняет — она в `Failed`, вместе с причиной; «повторить только их» — это новый прогон с теми
  же кодами, отдельной ручки не нужно. Неизвестный код туда же, а не 400 на весь прогон.
- Итог прогона — дифф, который живёт **12 часов**, а не 10 минут превью (прогон идёт час и
  больше). Рестарт API прогон теряет — принято планом.
- «Нет в источнике» сужено **до регионов прогона** (`RecordDiffService`): иначе при прогоне по
  одной стране все остальные страны категории `open` выглядели бы пропавшими.
- **ИНВАРИАНТ 1 — age/masters.** Держится сам собой: Apply только upsert, дифф берёт
  категорию из строк источника (`open`). **Не добавлять удаление «нет в источнике»:** у малых
  федераций наборы неполные, а у `country/ISR/open` есть строки федерации, которых World
  Aquatics не отдаёт, — удаление стирало бы чужие данные.
- **ИНВАРИАНТ 2 — Израиль.** На `country/ISR/open` пишут два источника с одним ключом,
  выигрывает применённый последним; порядок (И-13) — сперва World Aquatics, потом федерация.
  Поэтому `ISR` вылетает из прогона **даже если его попросили явно**
  (`RecordCountryRun.SkippedCode`, тест `Run_SkipsIsrael_EvenWhenAskedExplicitly`), а в статусе
  об этом сказано полем `IsraelSkipped`. Израиль обновляет только `--records-refresh`.

**Приёмка (живой прогон 16.09, AGU + JAM):** 2/2 страны, 175 строк, `failed` пусто, непринятых
строк 0; дифф — `added 86` (18 AGU + 68 JAM), `unchanged 86` (мировые), `missing 0`, категория
только `open`, регионы только `AGU`/`JAM`. Apply по странам не делался — заливка данных это шаг 6, и
`count(*)` по `Records` до сих пор прежний (ISR 781/726/90, world 86).
**Осталось для полной приёмки:** прогон с Apply на 3–5 странах и сверка `count(*)` ISR
age/masters/open до и после (шаг 6).

### 11.1.3 Качество кодов регионов ◐ (п. 1 и 4 ✅ 2026-09-15, п. 2 ✅ 2026-09-16)
Разведка (§3а) сузила задачу — отдельный экран карантина, скорее всего, не нужен:
1. ✅ **Баг `=WR` — сделан 2026-09-15 отдельно от фазы** (И-19): парсер, тесты
   `WorldRecordsEqualledRecordTests`, миграция `DropEqualledWorldRecordStrays`. Как было задумано: тип рекорда нормализовать с учётом
   `=` (повторённый рекорд) и составных значений; в WR-файле любая строка — `world`. После
   фикса `DeduplicateByAxes` склеит держателей равных времён: у 100 брасс ж 25 м к `LTU`
   (2013) добавится `JAM` (2014, 2016); у 100 на спине ж обе строки `USA` (2024 и 2025) —
   если это один человек, имя не изменится. Ожидаемо «изменится 1–2» в диффе. Затем удалить артефакты
   `country/USA/open` и `country/JAM/open`. Тест — фрагмент WR-отчёта с `=WR` (по образцу
   `WorldRecordsMedleyStyleTests`).
2. ✅ **`RegionCode` — из запроса. Сделано 2026-09-16.** `WorldAquaticsCountryFetcher`
   (шов `IRecordCountryFetcher`) качает по GUID страны два отчёта NR (SCM + LCM) и отдаёт
   строки `open` с кодом региона ИЗ ЗАПРОСА. Строка, где `NF Code` пуст или назвал другую
   федерацию, не пишется, а едет в `RecordCountryMismatchDto` — отчёт прогона (запрошенный
   код, что пришло, дисциплина, время). Фоллбек на колонку `Country` не используется вовсе.
   ⚠ Сверяем именно `NF Code` строки, а не тип рекорда: у строки «NR, WR» тип схлопывается
   в `WR`, и код страны по нему потерялся бы — национальный рекорд, который заодно мировой,
   пропал бы из прогона. Таймаут файла 120 с (замер — до 53 с), повторов два, и только на
   том, что проходит само: обрыв, таймаут, 5xx, 429; на 4xx — сразу наружу, кривой GUID
   источник не «починит». Микст-эстафеты пропускаются молча — они вне осей `Record`, это не
   находка. Тесты `WorldAquaticsCountryFetcherTests` — 7 на поддельных XLSX (ClosedXML,
   те же колонки) + живой под `SWIMM_NET_TESTS=1`: Ангилья отдала 18 рекордов, 0 непринятых
   строк.
3. Если отчёт покажет заметный объём несовпадений — тогда разбор в админке. По выборке их ноль.
4. ✅ **Сторож правдоподобия времени — сделан 2026-09-15** (И-20). Решение Влада: не
   блокировать, а метить через реестр спорных рекордов. Два правила (`RecordPlausibility`):
   рекорд страны / возраста быстрее мирового той же дисциплины; мировой улучшен за раз больше
   чем на 3 %. Apply пишет строку как в источнике и заводит её в реестр со статусом
   `candidate` — на сайте метки нет, пока человек не переведёт в `open`
   (`records-quality-plan.md` §3-бис). **Для Фазы 11:** порог улучшения для национальных
   рекордов подобрать по данным первого прогона (у малых федераций скачки 5–10 % нормальны).
**Приёмка:** в БД нет одиночек `USA`/`JAM`; повторённые WR лежат в `world` с двумя держателями;
несовпадения видны в отчёте прогона; невозможное время видно в превью и попадает в реестр
кандидатом.

### 11.1.4 Админка ✅ 2026-09-16
- На вкладке «Рекорды» — **одна главная кнопка «Прогнать все страны»**, без списка. Охват уже
  решён (§7, п. 1: все действующие страны), и селектор на видном месте задавал бы вопрос, на
  который ответ есть: пустой `RecordCountryRunRequest.Codes` и означает «все».
- Список стран в запросе нужен **не для выбора охвата**, а для двух узких вещей, и в UI им
  место второстепенное:
  - **«повторить упавшие»** — отдельного эндпоинта для ретрая нет сознательно: админка шлёт
    коды из `RecordCountryRunStatus.Failed` в тот же запуск (см. `RecordCountryRunDtos`).
    Без этого три отвалившиеся страны стоили бы повтора всех 215 — а прогон идёт час-два и
    не переживает рестарт API;
  - **одна страна для отладки** — посмотреть, что источник отдаёт по конкретному коду.
- Остальное на вкладке: статус фоновой задачи, дифф по странам перед Apply, таблица стран —
  код, строк, «обновлено» (`max(UpdatedAt)` по `RegionCode`), итог последнего прогона.
- ✅ 2026-09-16 **Дашборд `/Admin`, блок «Рекорды»:** плитка на каждый набор
  `(RegionType, RegionCode)` при 215 странах дала бы 215 плиток. Свёрнуто:
  `BuildRecordSetsAsync` оставляет мир и Израиль отдельными строками (за их свежестью следим
  мы), а остальные страны собирает в один синтетический набор
  `DashboardRecordSetStatus.OtherCountriesType` со счётчиком стран; фронт показывает его как
  «Другие страны · стран: N · обновлено …».
- Доки: `docs/admin-pages/import.md` и `index.md` (дашборд) — по `docs/pre-push-rules.md`.
**Приёмка:** Fetch→дифф→Apply для набора стран проходит из UI (нужен админ-логин — за Владом).

### 11.1.5 Масштаб и соседи ✅ 2026-09-16 (объём померен боевым прогоном)
- Объём ≈10–15k строк (§3а) против ~1.7k сейчас. Уникальный индекс по 8 осям уже есть.
  Кэш: ключи `records:{region}:{category}` работают как есть, Apply сбрасывает метку `Records`
  сам (К3/К4). Для рейтинга (11.2) — один ключ на **состав фильтра**, а не на регион.
- **Кто читает `Records` без фильтра по региону — проверить ДО первого боевого прогона:**
  - ✅ `RecordQualityService` — сужен до `world` + `ISR` (2026-09-16). Сверять рекорд Ямайки
    с нашими протоколами не с чем: это «сверять нечем», а не «не подтверждён». Фильтр один на
    сверку и на сводку (`Verifiable`), иначе «не проверено» стало бы равно числу иностранных
    рекордов.
  - ✅ Дашборд — свёрнут, см. 11.1.4.
  - ✅ Страница пловца ищет рекорды по имени держателя во всех регионах
    (`SwimmerPageRepository`, выборка `names.Contains(r.HolderName)`) — опасение было, что у
    иностранцев из наших протоколов (Маккабиада) появятся рекорды их стран. **Проверено на
    данных после заливки 16.09: совпадений ноль.** Запрос по всем четырём вариантам имени
    (оба порядка, HE и EN) против 13 788 иностранных строк не дал ни одной пары. Кода это
    по-прежнему не требует, но проверку стоит повторить, когда в базе появятся протоколы с
    заметным иностранным составом.
**Приёмка:** `/api/records?region=USA` в бюджете; сводка качества рекордов после прогона
не изменилась. Первая половина проверена на данных (шаг 6, 16.09); вторая обеспечена
фильтром `Verifiable` и закрыта тестом `Verify_SkipsForeignCountries`.

⚠ **Для витрины 11.2:** у 132 иностранных строк из 13 788 держатель пустой (`HolderName = ''`,
не NULL) — источник отдаёт время без имени. Рейтинг обязан такую строку показать, а не
пропустить и не напечатать пустое место.

---

## 5. Этап 11.2 — страница рейтинга рекордов

### 11.2.1 API ✅ 2026-09-16
`Records.TimeMs` уже есть — **вычисляемая** колонка (`GENERATED ... STORED` из `Time`),
так что сортировать и сравнивать можно прямо в запросе, а разъехаться с `Time` она не
может. Два разбора времени (SQL `swim_time_ms` и C# `SwimTime`) держит тест
`SwimTimeSqlContractTests` на 2615 живых значениях.

Новый `GET /api/records/ranking` (через `/api/records` нельзя — он требует `region`).
Фильтры: дисциплина (стиль × дистанция), бассейн 25m/50m, пол, регион(ы), сортировка по
времени, пагинация. **Категория фиксирована `open`** — в фильтрах её нет.
Требует решения по факту №4 (§3): парсинг `Time` в мс — на лету при выборке или
денормализованная колонка `TimeMs` (миграция). *Рекомендация: колонка `TimeMs` — сортировка
по строке иначе не выразима в SQL, а сортировка в памяти по 10–15k строк на каждый запрос
не нужна.*
**Сделано.** `GET /api/records/ranking` (`RecordsController`) → `IRecordRepository.GetRankingAsync`
→ `RecordRankingBuilder` (чистая функция: места и отставание). Ответ — дисциплина, `total`,
мировой рекорд отдельным полем и строки со своим местом.

- **Четыре оси дисциплины обязательны** (`style`, `distance`, `gender`, `pool`), иначе 400:
  рейтинг, где смешаны 50 и 100 метров или мужчины с женщинами, — не рейтинг. Категория
  зафиксирована `open` и параметром не вынесена вовсе.
- **Места спортивные:** равное время — равное место, следующее перепрыгивает группу
  (1, 2, 2, 4). Совпадения не редкость: в 50 в/с ж 25 м их 14 пар и 4 тройки.
- **Места считаются по ВСЕЙ дисциплине**, страницу репозиторий отрезает от готового рейтинга.
  Нумеровать с середины нельзя: страница, начавшаяся посреди группы одинаковых времён, соврала
  бы в первом же месте. Цена нулевая — в дисциплине не больше строки на страну (~215).
- **Отставание от мирового** в мс и процентах (делитель — мировое, чтобы проценты соседних
  строк были сравнимы). `null`, если мирового эталона нет (4×50 в длинной воде — проверено);
  **отрицательным быть может** и не обрезается: живые случаи И-22 и И-23.
- **Мировой рекорд несёт признак качества** (`issue_reason`) наравне со строками стран —
  инвариант И11, и это не теория: И-20 был ровно про кривой мировой рекорд. Поймано тестом
  `SwimTimeQualityInvariantTests` при первой сборке, а не глазами.
- **Нормализация осей — в `RecordRankingQuery.Create`**, не в контроллере: в базе дистанция
  эстафеты «4X50m» с заглавной X, и запрос «4x50m» молча вернул бы пустой рейтинг. Мимо
  контроллера в репозиторий ходят тесты и будущий 11.3.
- **Строки с неразобранным временем** в рейтинг не ставятся, но считаются отдельно
  (`unparsed_skipped`): «страны нет в рейтинге» и «её время не разобралось» — разные вещи.

**Приёмка пройдена 2026-09-16 на живых данных:**
- сортировка на смеси форматов — 100 в/с ж 50 м: …59.88 → 01:00.00 → 01:00.11…, все 196
  строк по возрастанию (строковая сортировка поставила бы минутные первыми);
- кэш по составу фильтра, коды стран в ключе сортируются — `?regions=USA,FRA,ISR` и
  `?regions=ISR,FRA,USA` дают одну запись кэша; ETag → 304 на повторе;
- EXPLAIN: seq scan, **2.4 мс**, 325 буферов на 16 685 строках. Индекс не заводили — таблица
  ограничена сверху (215 стран × ~90 дисциплин), а ответ живёт в кэше сутки;
- 50 в/с м 50 м: 209 стран, первый — AUS 20.88 (Макэвой, он же держит мировой, отставание 0),
  дальше BRA 20.91, FRA 20.94, USA 21.04 — сверено с известными рекордами;
- пустой держатель (эстафеты) едет в рейтинге, а не выкидывается — 107 таких строк найдено;
- проводка претензий проверена на живой базе: кандидат BER временно переведён в `open` —
  метка появилась на строке страны и НЕ протекла на мировой (проверка сдвига индекса),
  после чего статус возвращён в `candidate`.

**Тесты:** `RecordRankingBuilderTests` (чистая логика: ничьи, тройная ничья, отставание,
отрицательное отставание, отсутствие мирового, нормализация осей) + `RecordRankingPgTests`
(живая база: сортировка на смеси форматов, согласованность мест, фильтр стран, устойчивость
мест к пагинации, дисциплина без мирового, неизвестная дисциплина). InMemory здесь не
годится: `TimeMs` — вычисляемая колонка Postgres, на InMemory она осталась бы null.

⚠ **Для 11.2.2:** кандидаты сторожа на витрине не помечены (так решено — §3
records-quality-plan.md), поэтому **Бермуды сейчас стоят ПЕРВЫМИ в рейтинге 50 брассом ж
25 м с 27.72 — быстрее мирового 28.37, без единой пометки** (И-22). Страница обязана хотя бы
уметь показать отрицательное отставание как аномалию, иначе выглядит сломанной.

### 11.2.2 Страница `/records` ✅ 2026-09-16
`client/records.html` → `src/pages/records-page.tsx` → `projects/records-project/`
(оркестратор + `rk-discipline-picker`, `rk-world-card`, `rk-table`, модель дисциплин
`rk-disciplines.ts`). Тема deep — та же семья, что `/season-best`.

- **Дисциплина полосой кнопок, не селектами:** осей четыре (бассейн, пол, стиль, дистанция),
  значений у каждой единицы, и весь выбор виден целиком — сравнить 50 и 100 метров это один
  тап. Эстафеты отделены в ряду дистанций разделителем: «4×100m» рядом с «100m» читается как
  продолжение одной лестницы, хотя это другое соревнование.
- **Смена стиля чинит дистанцию:** 800 вольным → спина оставила бы дистанцию, которой у
  стиля нет, и таблица молча опустела бы. Берётся первая доступная.
- **Мировой рекорд — карточкой, а не первой строкой:** мир не участник рейтинга стран, и
  строкой наравне с ними означал бы, что он где-то занял первое место. Заодно видно, от чего
  считается «+0.08» у каждой строки.
- **«Israel #31 of 207» отдельной плашкой над таблицей:** в списке из двух сотен стран
  домашнюю подсветку ещё надо доскроллить, а вопрос «а мы где?» первый.
- **Отрицательное отставание показано как аномалия** — амбра в строке и в цифре, плюс
  объяснение в `title`: «быстрее мирового рекорда — два файла источника противоречат друг
  другу». Без этого рейтинг, где страна молча стоит выше мирового, читается как сломанный
  (живой случай И-22).
- **Одна разметка на десктоп и мобайл:** пять колонок складываются `grid-template-areas`,
  второго `*-mobile` компонента нет — он был бы вторым местом для правила подсветки.
- **Лестница дисциплин client-only** (`rk-disciplines.ts`): программа плавания не меняется,
  это справочник вида «какие заплывы бывают», как канонические ключи категорий. Эндпоинт под
  неё отдавал бы одно и то же годами, а страница платила бы лишним запросом.
- **Флаги:** карта `UI_FlagEmoji` дополнена до всех 212 стран справочника (было 98).

**Приёмка пройдена 2026-09-16 вживую:** 50 в/с м 50 м — AUS 20.88 первым, Израиль #22 из 209;
обе темы; мобильный экран (375) — строки складываются в карточки, панель дисциплины в столбик;
эстафета 4×100 в/с ж — составы четвёрок и метка `RELAY`; дисциплина без мирового (4×50 в/с
50 м) — карточка объясняет, что рекорд не ведётся, отставаний нет, в рейтинге одна строка;
ивритские имена держателей у Израиля рендерятся RTL внутри строки; консоль чистая; прод-сборка
включает `records.js`.

**Держатели латиницей — решение Влада 16.09.2026** (исключение из правила «имена ивритские
по умолчанию», записано в important.md). Экран международный: рекорды 212 стран приходят
латиницей, и одна ивритская строка посреди рейтинга читается как сбой кодировки, а не как
имя на родном языке.

Сделано подстановкой, не переводом: `HolderLatinName` (Application) + словарь «ивритское имя →
латинское» из карточек наших пловцов (`RecordRepository.HolderLatinNamesAsync`, свой ключ кэша
— словарь один на все дисциплины). Эстафета разбирается по запятым, каждый из четвёрки
отдельно: половина состава, которую мы знаем, читается. **Не нашли — оставляем иврит.**
Транслитерацию не придумываем: имя человека не угадывают. Тесты — `HolderLatinNameTests`.

**Источников латиницы ДВА, и порядок между ними не произвольный:**
1. **`Records.HolderNameEn`** (колонка, миграция `AddRecordHolderNameEn`, 16.09) — пришла
   вместе С ЭТОЙ строкой от источника и привязана к её времени, поэтому доверия больше;
2. словарь по карточкам наших пловцов — совпадение по ИМЕНИ, а значит теоретически возможен
   тёзка;
3. не вышло ничего — оставляем как в справочнике.

**Колонка решает то, что словарь не мог.** Латиница у нас была и терялась: `country/ISR/open`
пишут два источника, федерация ложится поверх World Aquatics (И-13) вместе с ивритским именем.
Доказательство лежало в базе — 2 строки из 90 остались латинскими, это эстафеты, которых у
федерации нет.

⚠ **Главное в колонке — не «заполнить», а вовремя СБРОСИТЬ** (`HolderLatinName.CarryOver`,
одно правило на импорт и обе админские формы; разбор — в
[../data-integrity.md](../data-integrity.md), «HolderNameEn»). Имя привязано к КОНКРЕТНОМУ
рекорду: сменилось время — рекорд другой, и прежнее английское имя сбрасывается. Иначе новый
рекорд оказался бы подписан именем предыдущего держателя, и заметить это можно было бы только
глазами.

**Замер после прогона 16.09:** латиницей стали **75 строк Израиля из 90** (было 36 на одном
словаре). Оставшиеся 15 упираются в данные: держателя нет в наших протоколах (из 29 держателей
личных рекордов в `Swimmers` есть 11) либо справочник пишет фамилию другим ивритским
написанием — «קריצ׳בסקי» в рекордах против «קריציבסקי» в карточке. Подгонять написания нельзя:
это угадывание имени человека.

### 11.2.3 Клиентский шов ✅ 2026-09-16
Страница ходит в `/api/records/ranking` напрямую и `RecordsHelper` не трогает вовсе — в
точке входа `records-page.tsx` нет даже `warmUp()`: прогревать справочник ОДНОЙ страны ради
рейтинга двух сотен значит платить лишним запросом на каждой загрузке. Легаси-дерево с ключом
`ISR→NR` осталось там, где и было (попапы нормативов, возрастные карточки), и ни одного его
файла правка не касается.

Заодно пункт **«Records» в топбаре перестал быть заглушкой**: он лежал `span`-ом «страницы не
существует» с тех пор, как топбар завели.

---

## 6. Этап 11.3 — head-to-head

### 11.3.1 API ✅ 2026-09-16
`GET /api/records/compare?a=ISR&b=USA[&pool=&gender=]` → `IRecordRepository.GetCompareAsync`
→ `RecordCompareBuilder` (чистая функция: выравнивание, исход, счёт). Разрез необязателен —
в отличие от рейтинга: смысл экрана в обходе ВСЕХ дисциплин сразу.

- **«Нет данных» — свой исход `no_data`**, а не ноль и не null-как-медленнее: строка остаётся
  видимой, `delta_ms` у неё `null`, и в счёт она не идёт ни в чью пользу.
- **Счёт считается по ОБЩИМ дисциплинам** (`compared`), а односторонние вынесены отдельными
  числами `a_only` / `b_only`. Это и есть 11.3.3 в структуре ответа, а не в комментарии.
- **Исход строкой** (`a` / `b` / `tie` / `no_data`), не числовым enum — та же тихая поломка,
  что ловил тест `RunStatus_SerializesStateAsString_NotNumber` у прогона по странам.
- **Дельта по модулю:** кто быстрее, сказано исходом, а знак пришлось бы читать вместе с тем,
  какая сторона слева.
- **Порядок дистанций числовой**, эстафеты после личных: строковая сортировка ставила бы
  «1500m» между «100m» и «200m».
- **Пара в ключе кэша НЕ сортируется:** «A против B» и «B против A» — зеркальные ответы
  (слева своя сторона), один ключ на оба отдал бы второму запросу чужую раскладку.
- `a == b` → 400: сравнение страны с самой собой не значит ничего.
- Рядом заведён **`GET /api/records/countries`** — коды, у которых есть рекорды В БАЗЕ, для
  выбора сторон. Не админский `/api/admin/records/countries`: тот ходит в живой источник за
  215 странами (включая пустые) и требует прав.

**Приёмка пройдена на живых данных:** ISR против HUN — 86 общих дисциплин, 10:76, и **4
дисциплины (4×50 эстафеты в длинной воде) только у Израиля**: они видны строками и в счёт не
идут. USA против AUS — 86 общих, 63:22, одна ничья (63+22+1=86).

**Тесты:** `RecordCompareBuilderTests` (перекос покрытия, ничья, зеркальность обмена сторон,
порядок дистанций, «нет данных» не поражение) + `RecordComparePgTests` (живая база:
согласованность счёта, исход против времён, зеркальность, разрез, несуществующая страна).

### 11.3.2 Страница `/records/compare` ✅ 2026-09-16
`client/records-compare.html` → `records-compare-project.tsx` + `rc-score-card`, `rc-table`.
Тема и стили общие со страницей рейтинга (`records-page.css`).

- **Слева домашняя страна, справа пусто.** «С кем сравнить» — это и есть вопрос экрана;
  подставлять за человека вторую страну значит отвечать за него.
- **Кнопка обмена сторонами** — ответ зеркальный, это закрыто тестами API и проверено в UI.
- **Строка «не сравнивались» стоит под счётом, а не сноской:** без неё «76:10» выглядит
  полным разгромом, хотя четыре дисциплины просто не с чем сравнить.
- **Сегмент `compare` в пути, пара в query.** Правилу routes.ts это не противоречит:
  `compare` — другой ЭКРАН тех же данных (как `/groups/{slug}/results`), а пара идентичностью
  не является — «ещё никто не выбран» законное состояние.

**Приёмка пройдена:** обмен сторонами даёт зеркальный счёт (`HUN 16 : 4 ISR` из `ISR 4 : 16
HUN`) и переписывает адрес; ссылка с параметрами воспроизводит экран целиком; десктопная
сетка и мобильная (складывается в карточку) проверены; консоль чистая; прод-сборка включает
`records_compare.js`.

### 11.3.3 Честность сравнения ✅ 2026-09-16 — требование, не косметика
- Даже внутри `open` у стран **разный набор дисциплин** и **разные даты** рекордов, где-то
  рекорда нет вовсе. **«Нет данных» ≠ «медленнее»** — иначе страница врёт в пользу той страны,
  по которой данных больше.
- Показывать `RecordDate` рядом со временем — иначе сравниваются величины разных эпох
  (нормализация формата — факт №5 в §3).
- В сводном счёте не засчитывать дисциплины, где у одной из стран данных нет.
**Приёмка:** страна с половинным покрытием не «выигрывает» пустотами.

**Как сделано — правило держится в четырёх местах сразу:**
1. в ответе — отдельный исход `no_data` и `delta_ms = null`;
2. в счёте — знаменатель `compared` (только общие), односторонние отдельными числами;
3. в таблице — прочерк и подпись «no record» вместо пустой ячейки, строка приглушена;
4. под счётом — «4 not compared (4 only ISR)» обычным текстом, а не мелким шрифтом.
Плюс дата рядом с каждым временем: иначе рекорд 2002 года читается просто как «медленнее»
(живой пример — 50 брассом м 50 м, HUN 27.51 от 02/08/2002 против ISR 27.02 от 22/06/2024).
Тест `Build_LopsidedCoverage_ScoreCountsOnlyCommonEvents` держит это на перекошенных данных.

---

## 7. Открытые вопросы

**Все четыре решены Владом 16.09.2026 — каждый по рекомендации. Открытых вопросов в фазе нет.**

1. **Все страны или подмножество?** ✅ **РЕШЕНО: тянем все действующие страны** (и в админке
   это кнопка «прогнать все», а не селектор — 11.1.4) — псевдо-сборные
   отсекаются сами (§3а), open-only объём ≈10–15k строк. Порог покрытия — вопрос витрины 11.2,
   не импорта. Боевой прогон всё равно начинается с 3–5 стран (шаг 6 в §8) — это приёмка, а не
   ограничение охвата.
   ⚠ **Дополнено 16.09.2026 по итогам боевого прогона (И-24): «реальных» ≠ «с непустым
   регионом».** Расформированные государства и нейтральные сборные проходили отсев насквозь,
   и после первой заливки в базе оказались ГДР, Саар, Чехословакия, Югославия и ещё восемь
   таких наборов. Решение Влада: убрать — и из списка прогона (явный список кодов в
   провайдере), и из базы. Цена решения записана там же: рекорд, который источник держит
   только под старым кодом, исчезает вместе с ним (400 к/п ж 50 м у Германии).
2. **Что делать с расхождением кода страны** (11.1.3). ✅ **РЕШЕНО: `RegionCode` берём из
   запроса** — мы знаем, чей GUID запросили. Несовпадения — строкой в отчёт прогона, без
   отдельного экрана разбора; июльский вариант «карантин + разбор в админке» отменён.
   Фоллбек на колонку `Country` отчёта запрещён и так (§4: это страна МЕСТА соревнования).
3. **`TimeMs`** (§11.2.1). ✅ **СДЕЛАНО 2026-09-16** — колонка миграцией `AddRecordTimeMs`, но ВЫЧИСЛЯЕМАЯ (`GENERATED ... STORED` из `Time`), а не заполняемая кодом: рекорды пишут четыре места плюс psql и дамп. Бэкфилл не понадобился: вычисляемая
   колонка посчиталась на всех 2897 строках сама. Подробности — data-integrity, «TimeMs».
4. **Израиль в батче** (11.1.2, инвариант 2). ✅ **РЕШЕНО: `ISR` в батче пропускаем** — Израиль
   только через `--records-refresh`, где порядок источников (И-13) гарантирован: сперва World
   Aquatics, потом федерация поверх.

## 8. Как вернуться к работе

1. Прочитать этот файл; при багах импорта — историю 2.6 в роадмапе (там выстраданное:
   `User-Agent`, дедуп equalled-рекордов, ловушка бэкофилла `timestamptz`) и И-13 в
   `docs/data-integrity.md` (кто владеет `country/ISR/open`).
2. Проверить, что не изменилось: `WorldRecordsSourceProvider` (откуда берётся countryId,
   таймаут), `WorldRecordsParser.NormalizeRecordType` (починен ли `=WR`), `RecordDiffService`
   (не появилось ли удаление «нет в источнике»), `Record` (появился ли `TimeMs`),
   `/api/records` (появился ли режим без `region`).
3. Порядок жёсткий: **11.1 до 11.2/11.3** — без данных страницы показывать нечего.

**Порядок работ внутри 11.1 (предложение 2026-09-15):**

| шаг | что | зачем именно сейчас |
|---|---|---|
| 0 | ✅ 2026-09-15 · Фикс `=WR` + удалить одиночки `USA`/`JAM` + тест (11.1.3 п. 1) | баг сегодняшнего импорта; маленький отдельный PR |
| 0б | ✅ 2026-09-15 · Сторож правдоподобия времени в диффе, пометка кандидатом в реестр (11.1.3 п. 4) | без него нынешний `--records-refresh` молча записал бы «мировой» 40.11 (И-20) |
| 1 | ✅ 2026-09-16 · Список стран из `/fina/countries` (11.1.1) — провайдер, отсев псевдо-сборных, тесты | без него нечего перебирать |
| 2 | ✅ 2026-09-16 · Провайдер «NR одной страны по GUID»: `RegionCode` из запроса, таймаут 120 с, повторы (11.1.3 п. 2) | кирпич, из которого собирается батч |
| 3 | ✅ 2026-09-16 · Фоновая задача: WR один раз + NR по странам, пауза, прогресс, дифф до Apply, **без ISR** (11.1.2) | 470 файлов не пролезут в HTTP-запрос |
| 4 | ✅ 2026-09-16 · Сузить сверку качества до `world` + `ISR`, свернуть дашборд (11.1.5, 11.1.4) | до первой заливки, иначе дашборд «сломается» |
| 5 | ✅ 2026-09-16 · Вкладка «Рекорды»: кнопка «прогнать все», прогресс, дифф, повтор упавших (11.1.4) | запуск из админки |
| 6 | ✅ 2026-09-16 · Боевой прогон: сначала 5 стран, потом все 234 · 13 593 строки, 0 упавших; затем чистка расформированных (И-24) | приёмка 11.1.2 |
| 11.1.6 | ✅ 2026-09-16 · Источник `wa-masters`: мастерские мировые рекорды из PDF worldaquatics (личные, LCM+SCM) | ось `world/masters` считалась несуществующей; заливки (Apply) ещё не было |
