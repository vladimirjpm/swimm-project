# Кэш: точность сброса до строки (К4б)

**Статус:** план от 14.09.2026. **К4б.0–К4б.4 сделаны** (14.09.2026): К4б.0–К4б.1 — PR #29 поверх
PR #28 К4 (CI зелёный); К4б.2 (метки строк при записи) — PR #30 поверх #29; К4б.3 (сужение при
чтении, сверка на попадании, страж, подметание) — PR #31 поверх #30; К4б.4 (страница группы на
блоках сужения) — PR #32 поверх #31, тесты 1904/1904. Дальше К4б.5 — клуб; до него — решение Влада
о дефолте выключателя (§10). **Продолжаешь — начни с §10** (ветки, PR, с чего начать К4б.5,
грабли). **Решение Влада (14.09.2026, ответ на §7-2
[cache-tags-plan.md](cache-tags-plan.md)): точность до строки — делать.** Начинать после
слияния К4 (ветка `cache-k4`): план стоит на его перехватчике сохранения. Вопросы плана решены
Владом в тот же день (§8): пловец — по замеру, выключатели — да, служебные колонки — да,
отдельным этапом, сверка на попадании в Development — да.

Как читать: §1 — что сейчас роняет страницы (живой снимок и разбор кода), §2 — схема меток и
почему она не даёт недосброса, §3 — страница за страницей, §4 — страховка, §5 — этапы с
приёмкой, §6 — чего план не лечит, §8 — решения.

## 0. Коротко

- **Сейчас** (после К4) запись сбрасывает метку своей таблицы: правка расписания группы 24
  роняет страницы ВСЕХ групп и обзоры ВСЕХ клубов (обзор клуба читает `HubGroups`).
- **Будет:** запись дополнительно сбрасывает метки строк (`row:HubGroups:24`), а страница одной
  сущности там, где запрос читает только её строки, зависит от строки, а не от таблицы. Правка
  группы 24 роняет страницу группы 24, список групп и обзор своего клуба — и больше ничего.
- **Плюс служебные колонки** (§2.6): правка, задевшая только колонку из короткого реестра
  (`Swimmers.LogligId*`, `Competitions.QualityScannedAt`…), роняет лишь записи кэша, в SQL
  которых эта колонка есть. Loglig-задания и проверка качества перестают ронять season-best и
  витрины «по всей стране».
- **Главный риск — недосброс**: страница решила, что читает «только свои строки», а прочла
  чужую. Страховка — одно узкое правило сужения (§2.3), тесты на каждое место сужения, журнал
  сбросов, обязательная сверка на попадании в Development и два выключателя (§4).
- **Где выгода:** группы (частые правки владельцев), обзор и состав клуба, всё, что задевают
  loglig и проверка качества. **Где мала:** сужение страницы пловца — делаем только по замеру
  журнала на проде (§3.3).
- **Самая дешёвая выгода — вообще без строк** (К4б.0): страница группы тянет `Sys_AppUsers` ради
  поля, которое контроллер выбрасывает, и поэтому КАЖДЫЙ вход через Google роняет страницы всех
  групп.

## 1. Как сейчас (живой снимок :5079 + разбор кода, 14.09.2026)

Метки страницы группы `http:hub-groups:group:{slug}:{visibility}` — 20 таблиц: `Clubs`,
`CompetitionEvents`, `Competitions`, `Countries`, `Galleries`, `GalleryItems`,
`HubGroupClubSubscriptions`, `HubGroupMembers`, `HubGroups`, `PointRulesClubs`,
`PointRulesClubsEntries`, `RelayMembers`, `Relays`, `Results`, `Styles`, `Swimmers`,
`Sys_AppUsers`, `Sys_HubGroupMedia`, `Sys_UserMedia`, `Sys_UserMediaPublications`. У обзора клуба
14 таблиц, среди них `HubGroups`; у записей пловца — `Swimmers`, `Results`, `RelayMembers`,
`Competitions`, `Styles`, `Clubs`, `Countries`, у части — `Records`.

**Кто роняет страницы одной сущности частыми записями** (пользователи, владельцы групп, фоновые
задания; импорт и так сбрасывает всё, админ — редко):

| Запись | Кто, как часто | Таблица | Роняет сейчас | После плана |
|---|---|---|---|---|
| Вход через Google (`AuthController` ~88-119: `DisplayName`, `AvatarUrl`, `UpdatedAt`) | каждый вход | `Sys_AppUsers` | страницы всех групп | ничего: страница группы перестаёт читать `Sys_AppUsers` (К4б.0), а если имя и аватар те же, меняется только служебный `UpdatedAt` (§2.6) |
| Расписание, правка, политика вступления, оформление группы; `TouchGroupAsync` (`UpdatedAt` после каждой правки состава); синхронизация подписки | владелец, часто | `HubGroups` | все группы + обзоры всех клубов + список | своя группа + список + обзор своего клуба |
| Добавить/убрать/скрыть участника, синхронизация по подписке | владелец | `HubGroupMembers` | все группы + список | своя группа + список |
| Подписка на клуб | владелец | `HubGroupClubSubscriptions` | все группы + список | своя группа + список |
| Медиа группы | владелец | `Sys_HubGroupMedia` | все группы | своя группа |
| Подать / отозвать публикацию, решение модератора | пользователь / владелец | `Sys_UserMediaPublications` | все группы | группа-цель |
| Добавить медиа в My media | пользователь | `Sys_UserMedia` | все группы | ничего, пока не опубликовано |
| Удалить медиа | пользователь | `Sys_UserMedia` + каскад в публикации | все группы | все группы (каскад, §2.2-5, §6) |
| Loglig-подсказка (`POST /api/swimmers/{id}/loglig-suggest`), суточные loglig-задания, штамп loglig после импорта и забора стартовых протоколов | пользователь / фон | `Swimmers` (колонки `LogligId*`) | все группы, клубы, пловцы, season-best… | только записи, в SQL которых есть `LogligId*` (§2.6), и суженные записи этого пловца |
| Прогон проверки качества (`SuspectResultService`, штамп `QualityScannedAt`) | админ / после импорта | `Competitions` | почти всё | только записи, читающие `QualityScannedAt` (§2.6) |

⚠ Поправка к §7-2 cache-tags-plan: там было сказано, что правка группы не роняет клубы. Роняет:
обзор клуба читает официальную группу клуба из `HubGroups` (`ClubOverviewRepository` ~116-119).

## 2. Схема

### 2.1 Метки

| Метка | Смысл | Кто сбрасывает | Кто от неё зависит |
|---|---|---|---|
| `table:T` (есть) | изменилась какая-то строка `T` | любая запись в `T`, кроме служебной (§2.6) | записи кэша, читавшие `T` целиком (списки, рейтинги, season-best) |
| `row:R:id` (с К4б.2 сбрасывает запись; зависеть начнут с К4б.3) | изменилась строка корня `R` с этим id **или её прямой потомок** | запись, которой id известен: своя строка корня, FK на корень | записи, сузившие чтение до этих id |
| `anyrow:T` (новая) | изменились строки `T`, но какие — неизвестно | массовая запись, каскад в базе, сжатие больших сохранений | записи, сузившие чтение `T` (вместе с `row:`) |
| `col:T.C` (новая, §2.6) | в `T` изменилась служебная колонка `C` | правка, задевшая только служебные колонки | записи, в SQL которых есть `"T"` и `"C"` |

Суженная запись кэша теряет `table:T` и получает `row:R:id` + `anyrow:T`. Отсюда весь выигрыш:
обычное сохранение чужой строки сбрасывает `table:T` и `row:R:{чужой id}` — суженную запись не
задевает ни то, ни другое.

**Корни сужения** — короткий реестр в одном месте (рядом с `CacheTags`): `HubGroups`, `Clubs`,
`Swimmers`, `Sys_UserMedia`, `Relays`. Метки строк рождаются только для корней: остальные (свой id
у `Results` никто не сужает) лишь раздували бы каждое сохранение. Сужение по корню вне реестра —
исключение, а не метка, которую никто никогда не сбросит.

### 2.2 Запись: перехватчик сохранения (`CacheInvalidationInterceptor`)

Для каждой изменённой строки таблицы `T`, кроме `table:T` (как сейчас):

1. `T` — корень → `row:T:{PK}`.
2. Каждый FK строки на корень `P` → `row:P:{значение}`. У изменённой строки — **и старое, и новое**
   значение (участник ушёл из группы 24 в 17 → обе; результат переписали на другого пловца →
   оба). У удалённой — старое. `null` пропускается.
3. **Временные значения пропускаются.** Ключ, который выдаст база (новая группа и её участники в
   одном сохранении), принадлежит строке, которой ещё нет, — от неё не может зависеть ни одна
   запись кэша. Поэтому всё считается в `SavingChanges`, как сейчас; перечитывать ключи после
   сохранения не нужно.
4. **Сжатие:** одно сохранение меняет больше N строк одной таблицы (N ≈ 500) → вместо их меток
   строк `anyrow:T`. Слияние клубов (≈1,5 тыс. результатов), пересчёт объединённых мест, перенос
   результатов — редкие админские операции; метки на тысячи строк им ни к чему, а в Redis каждая
   метка — `INCR`.
5. **Каскад в базе** (удаление с `Cascade`/`SetNull`): таблица потомка `C` уже даёт `table:C` (К4),
   теперь ещё `anyrow:C` — какие строки задел каскад и к каким корням они относятся, трекер не
   знает. Удалённая строка корня свою `row:` даёт сама (п. 1), и её прямых потомков, суженных по
   этому корню, это закрывает. `anyrow:C` нужен для потомков, суженных по ДРУГОМУ корню: удалили
   медиа — ушли его публикации, а страница группы сужена по группе.
6. **Явный сброс массовой записи** (`ExecuteUpdate`/`ExecuteDelete`, сырой SQL) — `table:T` +
   `anyrow:T` одним методом (`db.InvalidateTableCacheAsync<T>()`). Старый вызов
   `InvalidateCacheTagsAsync(db.TableTag<T>())` дал бы одну `table:T` и прошёл бы мимо суженных
   записей — это недосброс, поэтому старый вызов убрать. Мест три: стирание объединённых мест в
   пересчёте, перестановка избранного, чистка истории входов. (Сделано в К4б.2: старого метода
   больше нет.)
7. **Строка не из запроса — строки неизвестны** (найдено при К4б.2). Старое значение FK верно,
   только если строку загрузил запрос: тогда исходные значения трекера — из базы. Заглушка
   (`Remove(new HubGroupMember { Id = 100 })`), `Update(объект с формы)`, повторное подключение
   после `ChangeTracker.Clear()` несут исходные значения, которые дал код: у заглушки
   `HubGroupId = 0`, и `row:HubGroups:24` потерялась бы — недосброс. Поэтому контекст с рождения
   следит, какие строки подключены не запросом (событие `ChangeTracker.Tracked` с
   `FromQuery = false`, кроме добавления; `CacheInvalidationInterceptor.Watch` из конструктора
   `SwimmDbContext`), и изменённая или удалённая такая строка с FK на корень даёт `anyrow:T`
   вместо меток строк. Своя метка корня от этого не зависит: ключ у заглушки настоящий. В коде
   таких записей сейчас нет (проверено 14.09.2026: ни `Attach`, ни `Update`, ни заглушек) — это
   страховка на будущее, и она в безопасную сторону: контекст, который не следит, не верит
   никому.

Отметка «был онлайн» (`LastSeenAt`, массовый UPDATE) по-прежнему ничего не сбрасывает (§3.2
cache-tags-plan).

### 2.3 Чтение: сужение

Сужает репозиторий (Infrastructure — там и запросы, и модель); контроллеры меняются только там,
где надо передать id:

```csharp
// Запросы внутри блока читают из этих таблиц ТОЛЬКО строки группы groupId.
using (_read.CacheRows<HubGroup>(groupId, typeof(HubGroupMember), typeof(HubGroupMedia)))
{
    ...
}
```

Внутри блока перехватчик команд (`CacheDependencyInterceptor`) касается не `table:T`, а
`row:HubGroups:{id}` + `anyrow:T` — для перечисленных таблиц и самого корня; остальные таблицы в
том же SQL — как раньше, `table:X`. Вне сборки записи кэша блок ничего не делает (тот же метод
зовут и некэшированные пути).

**Правило — единственное, на чём держится корректность.** Таблицу в блоке можно перечислить,
только если КАЖДЫЙ запрос блока, который её читает, фильтрует её строки по FK на корень
(`m.HubGroupId == id`) или по id корня — и никакой другой экземпляр этой таблицы в том же SQL
чужие строки не читает. Почему этого хватает: раз фильтр содержит `FK = id`, любая строка, которая
была в выборке или может в неё войти, имеет это значение FK до записи или после — а перехватчик
сбрасывает оба (п. 2 §2.2). Приёмы:

- **JOIN не сужают — делят запрос.** Публикации группы вместе с медиа одним SQL — две таблицы разной
  природы. Сначала публикации по группе (сужено), потом медиа по списку id (корень
  `Sys_UserMedia`), потом подписи результатов (таблицей).
- **Id корня — ДО запроса.** Метка снимается в момент касания (как у таблиц, `CacheBuildScope`), а
  касание — до чтения. Поиск по slug внутри блока сузить нельзя: id ещё неизвестен. Страница группы
  уже знает id из проверки доступа (`HubGroupsController` ~116) — загрузка переходит на id.
- **Потомок — только прямой.** `row:HubGroups:24` сбрасывают строки с FK на группы. Внук без своего
  `HubGroupId` сужается по родителю, если тот в реестре корней, или не сужается вовсе.
- **Вложенная сборка сужение НЕ наследует.** Блок живёт в `AsyncLocal` и без явного сброса утёк бы
  во вложенную `GetOrCreateAsync`: общая запись (скажем, «все стили»), собранная внутри блока
  группы 24, легла бы в кэш с меткой группы 24 и потом врала бы всем. `BuildAsync` открывает
  сборку с чистым сужением. Метки вложенной записи наружу наследуются, как раньше, — `row:` тоже.
  (Сделано в К4б.3 привязкой, а не сбросом: блок помнит сборку, в которой открыт, и
  `CacheBuildScope.TouchTable` блок чужой сборки не применяет — у вложенной свой объект сборки.)
- **Вложенный блок** на своё время заменяет внешний, на выходе внешний возвращается (К4б.3).
  Чтение, которое внутренний блок не сужает, зависит от таблицы — лишний сброс, не недосброс.
  Объединять блоки нельзя: запросы внутреннего не обязаны фильтровать по FK внешнего.
- **Потолок:** больше M id в одном блоке (M ≈ 200) — блок работает как таблица. Сотня меток у
  страницы группы нормальна; тысяча — уже не выгода, а нагрузка (особенно для Redis, §7).
- **Проверка по модели:** у каждой перечисленной таблицы должен быть FK на корень (или она сама
  корень), иначе исключение — в тестах всплывёт сразу.
- **Почему не `TagWith`:** пометить сам запрос строкой с id соблазнительно, но аргумент `TagWith` —
  константа для EF (`[NotParameterized]`): на каждый id свой скомпилированный запрос, кэш
  запросов EF забивается.

### 2.4 Почему не будет недосброса

Суженная запись страницы группы 24 зависит от `row:HubGroups:24` и `anyrow:` своих таблиц.

| Запись | Её метки | Страница 24 |
|---|---|---|
| правка группы 24 | `table:HubGroups`, `row:HubGroups:24` | падает ✓ |
| правка группы 17 | `table:HubGroups`, `row:HubGroups:17` | живёт ✓ |
| новый участник в 24 | `table:HubGroupMembers`, `row:HubGroups:24` (FK) | падает ✓ |
| участник переехал 24 → 17 | `row:HubGroups:24`, `row:HubGroups:17` | падает ✓ |
| новая группа | `table:HubGroups` (временный id пропущен) | живёт ✓ — от несуществующей строки никто не зависит |
| служебный `UpdatedAt` группы 17 | `col:HubGroups.UpdatedAt`, `row:HubGroups:17` | живёт ✓ |
| массовая запись в `HubGroupMembers` | `table:…`, `anyrow:HubGroupMembers` | падает ✓ |
| удаление пловца (каскад в участников) | `table:…`, `anyrow:HubGroupMembers` | падает ✓ |
| импорт, настройки, кнопка | `all` | падает ✓ |

Недосброс возможен ровно в одном случае — нарушено правило §2.3 (или, для колонок, SQL прочёл
колонку, не назвав её, — §2.6). Против этого — §4.

### 2.5 Выключатели (решено: да)

Две настройки на `/Admin/Settings`, каждая включается после приёмки своего этапа:

- **`CacheRowPrecision`** (сделан в К4б.3) — выключено: блоки сужения ничего не делают, записи
  зависят от таблиц.
- **`CacheColumnPrecision`** (К4б.6) — выключено: служебная правка даёт `table:T`, как обычная.

Ключи — по обычаю `/Admin/Settings` (PascalCase, константы `CacheSettings`); в первой редакции
плана звались `cache.rowPrecision` / `cache.columnPrecision`. Там же доля сверки на попадании —
`CacheHitVerifyPercent` (§4-6).

Два, а не один: всплыл недосброс — выключаешь по одному и сразу видишь, чей он. Смена настроек уже
сбрасывает весь кэш, смеси старого и нового не будет. Это аварийный рычаг: выключить за секунду и
чинить спокойно, без отката релиза. Метки строк запись сбрасывает при любом положении — лишний
сброс никому не мешает.

### 2.6 Служебные колонки (решено: да, отдельным этапом К4б.6)

Узкое отступление от «по колонкам не дробим» (§3.3 cache-tags-plan): не колонки вообще, а
короткий **реестр колонок, которые часто пишут и мало кто читает**.

- **Реестр** — рядом с корнями, свойства сущностей, имена колонок берутся из модели. Стартовый
  список: `Swimmers.LogligId`, `LogligIdStatus`, `LogligIdSource`, `LogligIdSuggestedByUserId`,
  `LogligIdSuggestedAt`, `LogligIdVerifiedAt`; `Competitions.QualityScannedAt`;
  `Results.CombinedPlace`, `IsBestResult`, `BestTimeMs`; `HubGroups.UpdatedAt`;
  `Sys_AppUsers.UpdatedAt`. Окончательный — по журналу К4б.1: в реестр идёт колонка, чья запись
  часто выкидывает записи, которые её не читают. Свойство вне модели — исключение.
- **Запись.** Изменённая строка, у которой ВСЕ изменённые свойства из реестра, даёт `col:T.C` на
  каждую изменённую колонку и метки строк по §2.2, но **не** `table:T`. Всё прочее — как обычно:
  есть хоть одна не-служебная колонка, добавление, удаление, `Update()` (помечает изменёнными все
  свойства). Массовая запись только служебных колонок — явным методом с перечнем колонок →
  `col:` + `anyrow:T` (стирание объединённых мест в пересчёте — как раз такое).
- **Чтение.** Перехватчик команд касается `col:T.C`, если в тексте SQL есть и таблица `"T"`, и
  колонка `"C"` — так же, как метки таблиц в К3. Совпадение не по делу (такая же колонка у другой
  таблицы запроса) даёт лишний сброс, а не ошибку. Внутри блока сужения суженным таблицам `col:`
  не нужен: их закрывают `row:`/`anyrow:`.
- **Почему безопасно.** EF всегда перечисляет колонки по именам — в SELECT, WHERE и ORDER BY.
  Если запрос колонку не назвал, он её не читал, и правка одной этой колонки его ответ не меняет.
  Представлений и чтения сырым SQL в модели нет (проверено 14.09.2026). На будущее перехватчик
  считает `SELECT *` и `"x".*` чтением всех служебных колонок упомянутых таблиц.
- ⚠ **Выгода только у проекций.** Сущность, загруженная целиком (материализация без `Select`,
  `Include`), называет в SQL все свои колонки и получит `col:` — это правильно (она их прочла), но
  падать будет по-прежнему.
- **Что даёт.** Loglig-подсказка, суточные loglig-задания и прогон проверки качества перестают
  ронять season-best, ровесников, обзоры клубов и страницы групп: эти записи служебных колонок не
  читают. Пересчёт объединённых мест роняет только тех, кто показывает места. Вход через Google
  не роняет ничего, если меняется только `UpdatedAt`.

## 3. Страница за страницей

Номера запросов (Q, O, R…) и строки файлов — по коду на 14.09.2026; перед правкой сверить.

### 3.1 Группа — `GET /api/hub-groups/{slug}` (единственная кэшированная запись о группе)

Фабрика: `HubGroupsController` ~131-174 → `HubGroupPublicRepository.GetBySlugAsync`,
`HubGroupMediaService.GetGalleryAsync`, `UserMediaPublicationService.GetApprovedForGroupAsync`.
Вложенных записей кэша нет. Вкладка результатов группы — свои записи по списку пловцов, таблиц
групп не читает; тренировки, медиа участников, публикации — не кэшируются.

| Запрос | Сейчас читает | Как будет |
|---|---|---|
| Q1 группа `WHERE Slug` + клуб, страна | `HubGroups`, `Clubs`, `Countries` | загрузка по id (из проверки доступа) в блоке `HubGroup`; `Clubs`, `Countries` — таблицей |
| Q2 участники `WHERE HubGroupId` + пловцы и их клубы | `HubGroupMembers`, `Swimmers`, `Clubs` | `HubGroupMembers` сужено; `Swimmers`, `Clubs` — таблицей (пловцов читают и Q5–Q10, там сузить нельзя) |
| Q3 подписка + официальная группа клуба подзапросом | `HubGroupClubSubscriptions`, `Clubs`, ⚠ **`HubGroups` чужой строки** | делим: Q3a подписка `WHERE HubGroupId` (сужено по группе) → id клуба `c`; Q3b официальная группа `WHERE ClubId = c AND IsOfficial` в блоке `Club`(c) по `HubGroups` → `row:Clubs:c`. Её правку, удаление, одобрение другой группы официальной и перенос `ClubId` поймает п. 2 §2.2: у строки группы FK `ClubId` |
| Q4–Q12 заплывы состава, последний старт, рекорды группы, зачёт, правила очков | `Results`, `Competitions`, `CompetitionEvents`, `Styles`, `Relays`, `RelayMembers`, `Galleries`, `GalleryItems`, `PointRulesClubs(+Entries)`, `Swimmers`, `Countries`, `Clubs` | таблицей: пишут импорт и админ, а эстафеты тянут чужих пловцов |
| Q13 медиа группы `WHERE HubGroupId` | `Sys_HubGroupMedia` | сужено |
| Q14 публикации `WHERE HubGroupId` + медиа, владелец, пловец, результат | `Sys_UserMediaPublications`, `Sys_UserMedia`, `Sys_AppUsers`, `Swimmers`, `HubGroups`, `Clubs`, `Results`, `Styles`, `Competitions` | К4б.0: публичная проекция без владельца, пловца и названия цели (контроллер ~147-154 берёт только Id, MediaType, SourceType, Url, ResultLabel). К4б.4: делим — публикации по группе (сужено) → медиа по списку id (корень `Sys_UserMedia`) → подписи результатов (таблицей) |

Итог по меткам группы 24: `row:HubGroups:24`, `row:Clubs:{клуб подписки}`,
`row:Sys_UserMedia:{…}`, `anyrow:` таблиц групп и медиа; `table:` — только данные, которые пишут
импорт и админ, плюс `Swimmers` (служебные loglig-правки его уже не сбрасывают, §2.6). Ни одной
таблицы групп и медиа целиком, и нет `Sys_AppUsers`.

Таблицей читают группы и дальше: список групп `/api/hub-groups`, дашборд `dashboard:status`. Правка
любой группы роняет их — одна-две записи, так и надо.

**Сделано в К4б.4 — где вышло иначе, чем в разборе выше:**

- **Q1 — по id И slug** (`HubGroupPublicRepository.GetPageAsync(id, slug)`; бывший `GetBySlugAsync`).
  Id приходит из проверки доступа ДО сборки, а ключ записи — slug: переименуй группу между ними, и
  под старым slug легла бы страница другой группы. Фильтр `Id AND Slug` правилу сужения не мешает
  (фильтр по id корня есть), а гонку превращает в «группа исчезла» — не кэшируется.
- **Q14 — два запроса, не три** (`UserMediaPublicationService.PublishedItemsAsync`): публикации
  цели (блок группы) → медиа по списку id вместе с пловцом и подписью заплыва. `Swimmers`,
  `Results`, `Styles`, `Competitions` в том же SQL остаются табличными — сужена только
  `Sys_UserMedia`, так что отдельный третий запрос ничего не дал бы. Тем же кодом идёт лента клуба
  (`GetApprovedForClubAsync`) — она не кэшируется, поведение не меняется.
- Живые метки на :5079 совпали с «итогом» выше: у группы 17 — `row:HubGroups:17`,
  `row:Sys_UserMedia:18`; у группы 24 — `row:HubGroups:24`, `row:Clubs:438`; `anyrow:` таблиц групп
  и медиа; `table:` — только данные и `Swimmers`, `Clubs`, `Countries`.

### 3.2 Клуб

Четыре записи (`ClubsPublicController`): обзор, состав, season-best, стена рекордов. Id клуба
известен до сборки (`ResolveClubIdAsync` снаружи, склеенный клуб резолвится в канонический).

- **Обзор** — сужаем ровно одно место: официальная группа клуба (`ClubOverviewRepository`
  ~116-119, `HubGroups WHERE ClubId = rid AND IsOfficial`) — блок `Club`(rid) по `HubGroups`. Это
  единственная таблица клуба с частыми записями: сейчас любая правка любой группы роняет обзоры
  всех клубов; будет — только своего клуба, а у большинства пользовательских групп `ClubId` пуст,
  то есть ничьего. Остальное в обзоре — таблицей: рейтинг по всем клубам соревнования (O9),
  season-best по всей стране (O8), стена рекордов по именам (O7); сужать нечего, пишут импорт и
  админ (а шум loglig и проверки качества снимают служебные колонки, §2.6).
- **Состав** — сужается целиком: `Swimmers WHERE ClubId` и `Results WHERE ClubId AND SwimmerId IN
  (…)` — блок `Club`(rid) по `Swimmers`, `Results`. Правка пловца клуба X роняет состав только X;
  переход пловца в другой клуб — старый и новый `ClubId` (п. 2).
- **Season-best клуба** — не сужается: лидер ступени выбирается по всей стране, фильтр по клубу
  идёт в памяти уже после (`ClubPublicRepository` ~328).
- **Стена рекордов** — `Clubs` сузился бы (строка клуба и склеенные дубли: `MergedIntoId` — тоже FK
  на `Clubs`, п. 2 поймает склейку), но `Records` и `Sys_RecordIssues` читаются целиком, а
  `Clubs` пишет только админ. Выгоды нет, не трогаем.

### 3.3 Пловец — по замеру (решено)

Выгода мала: в таблицы, из которых собраны записи пловца, пишут импорт (сбрасывает всё), админ и
фон, а самый частый шум — loglig — снимут служебные колонки (§2.6). **Решение: сужение пловца
делаем, только если журнал сбросов на ПРОДЕ покажет, что записи `http:swimmer:*` часто
выкидывает что-то, кроме импорта и общего сброса.** Локальная база для такого замера не годится —
на ней нет живого трафика; смотреть после деплоя (docs/plans/azure-deploy-plan.md).

Что сузилось бы (разбор на 14.09.2026, чтобы не повторять):

- `swimmer-swims:{id}` — личные `Results` и `RelayMembers WHERE SwimmerId` (корень `Swimmers`) и
  ⚠ эстафетные `Results WHERE RelayId IN (…)`: это строки первого этапа, обычно чужие, по
  `SwimmerId` их сужать НЕЛЬЗЯ. Корень `Relays` по списку эстафет пловца (п. 2 даёт
  `row:Relays:{RelayId}` из `Results`, `RelayMembers` и `Relays`).
- `swimmer-profile:{id}` — строка пловца (клуб и страну — таблицей).
- `athlete-career-id:{id}`; предстоящие старты (`CompetitionEntries WHERE SwimmerId IN`).
- HTTP-записи профиля, summary, best-times, progress собраны из них, плюс `Competitions`,
  `Styles`, категории и рекорды — таблицей.

Не сужаются: personal-bests, season-ranks, compare. Они вкладывают записи клуба (`club-best-ms`)
и ровесников (`age-cohort-season-bests`), которые читают `Results` и `Swimmers` целиком.

Попутно, независимо от замера: в compare профиль соперника читается ДО сборки
(`SwimmersPublicController` ~298), его метки в запись не попадают — перенести внутрь фабрики
(в К4б.0).

## 4. Чем страхуемся от недосброса

1. **Тесты записи** (InMemory с настоящей моделью `SwimmDbContext`: перехватчику сохранения SQL не
   нужен): своя и чужая строка, FK старое+новое, `null`, временный id, каскад → `anyrow`, сжатие,
   только корни из реестра, массовый метод даёт `anyrow`, строка не из запроса (заглушка,
   `Update`, повторное подключение) → `anyrow`, а `Update()` строки из запроса — точно (§2.2 п. 7);
   служебная правка → `col:` без `table:`, смешанная → как обычно. Сделано для строк в К4б.2
   (`CacheRowTagsTests`); временные id выдаёт только реляционный провайдер — их тест идёт на
   Npgsql без соединения (`CacheInvalidationInterceptor.PreviewTags`).
2. **Тесты чтения** (живой Postgres, только чтение — как `CacheDependencyInterceptorPgTests`):
   настоящая страница группы/клуба собирается в кэш, проверяем метки: есть `row:HubGroups:{id}`,
   нет `table:HubGroups`, нет `table:Sys_AppUsers`; вложенная сборка внутри блока не получает
   `row:`; проекция без колонки не получает `col:`, с колонкой — получает. К4б.3: механизм на
   настоящем SQL — `CacheRowNarrowingTests.RealSql_…` (участники в блоке группы → `row:` +
   `anyrow:`, пловцы из JOIN — `table:`); временные таблицы не понадобились, тест только читает.
   Страницы целиком — с К4б.4: `CacheGroupPageNarrowingTests.RealPage_…` (все группы базы), там же
   выключатель на настоящей странице и сверка на попадании без расхождений.
3. **Сценарные тесты** стыкуют 1 и 2: метки InMemory-сохранения сбрасываем в кэше, где лежат
   настоящие страницы из PG. «Правка группы B не роняет A», «правка A роняет A», «новый участник
   A», «публикация в A», «медиа добавлено без публикации — A живёт», «группу одобрили официальной
   для клуба, на который подписана A, — A падает», «массовая запись — падает», «loglig-правка
   пловца — season-best живёт». Живую базу тесты не пишут. Сделано для группы в К4б.4
   (`CacheGroupPageNarrowingTests`, 12 сценариев): страницы — из живой базы, записи — в
   InMemory-зеркало с теми же id, его перехватчик сбрасывает метки в тот же кэш. Loglig — в К4б.6.
4. **Страж мест сужения** (как `NoManualSetAsync`): IL-скан вызовов `CacheRows<…>`, каждое место
   обязано стоять в списке сценарного теста. Новое сужение без сценария роняет тесты. Сделан в
   К4б.3: `CacheRowsSitesTests`, список `Covered` («Тип.Метод»); сканер IL вынесен в `IlCalls`
   (им же пользуется `CacheValueRulesTests`). С К4б.4 в списке четыре места страницы группы.
5. **Журнал сбросов** на `/Admin/Cache` (К4б.1): последние ~200 сбросов — время, источник (какие
   таблицы и колонки поменяло сохранение), метки, сколько и какие записи выкинуты. Им меряем
   «до/после», выбираем служебные колонки и принимаем вживую.
6. **Сверка на попадании — обязательна в Development (решено).** Попали в запись с метками
   `row:` или `col:` — с вероятностью p (настройка, в Development по умолчанию ощутимая, на проде
   выключена) ответ строится заново **целиком мимо кэша**, включая вложенные записи, в
   изолированной сборке, и JSON сравнивается (у `CachedJson` достаточно ETag). Не совпало —
   строка «подозрение на недосброс» в журнале (ключ, метки, первое расхождение) и сброс записи.
   Ловит то, чего не предусмотрели тесты, при обычном прокликивании на :5079. Сверяем только
   записи с `row:`/`col:` — табличные доказаны К4, а шум «от времени» (относительные даты) в
   остальных нам не нужен. Сделана в К4б.3 (`MemoryCacheService.MatchesFreshAsync`): доля —
   настройка `CacheHitVerifyPercent` (Development 20, прод 0); сверяются записи с `row:`/`anyrow:`;
   сборка-сверка (`CacheBuildScope.BeginVerification`) вложенные записи строит заново и ничего не
   кладёт; расхождение — запись вон и пересборка обычным путём, в журнале — блок «Сверка на
   попадании» (счётчики и последние 50 расхождений). Запись, сброшенная, пока её сверяли, —
   не подозрение (данные поменялись законно); упавшая сверка страницу не роняет.
7. **Выключатели** (§2.5).

## 5. Этапы

| # | Что | Приёмка |
|---|---|---|
| ✅ К4б.0 | **Лишние зависимости, без строк.** Ленты зрителя — отдельная проекция `PublishedMediaItemDto` (без владельца медиа и без названия цели): её отдают страница группы, лента группы (public и members) и лента клуба; строка inbox-а с email владельца осталась только модераторам (docs/media-page.md). `SwimmerProfileDto.CopyForPage()` — страница пловца дописывает шапку в копию, а не в объект из кэша; профиль соперника в compare читается внутри фабрики | ✓ PG-тест `GroupPage_DoesNotDependOnAppUsers_ModerationInboxDoes` (мутация «Contains» роняет его — проверено); ✓ `PublicFeeds_DoNotCarryTheMediaOwner`, `CopyForPage_…`; ✓ вживую: у страниц групп 19 таблиц вместо 20, `Sys_AppUsers` нет; ленты public (1) и members (9) отдают 9 полей зрителя, «owner» нигде |
| ✅ К4б.1 | **Журнал сбросов**: `ICacheDiagnostics.Journal()` (последние 200 сбросов, выкинувших хоть одну запись, + пустые счётчиком + «кого выкидывают» по видам записей); подпись — перегрузки `InvalidateTagsAsync(tags, reason)` / `InvalidateAllAsync(reason)`, у перехватчика — таблицы, `~/+/−` и изменённые колонки, в транзакции одной строкой на коммите; шесть общих сбросов подписаны; раздел на `/Admin/Cache` (+ `docs/admin-pages/cache.md`) | ✓ 10 тестов журнала в `MemoryCacheServiceTests`, 2 — подписи в `CacheInvalidationInterceptorTests` (одна — транзакция на живом PG); ✓ вживую: правка расписания группы 24 → «SaveChanges: HubGroups ~1 (TrainingSchedule, UpdatedAt)», выкинуто 4 (список, группы 24 и 17, обзор клуба 438); возврат расписания — пустой сброс |
| ✅ К4б.2 | **Метки строк при записи** (§2.2): реестр корней `CacheRowRoots` (Infrastructure), `CacheTags.AnyRow`, п. 1–7 в `CacheInvalidationInterceptor` (сжатие больше 500 строк — и в сохранении, и в сумме по транзакции), `InvalidateTableCacheAsync<T>()` вместо трёх явных сбросов, старый `InvalidateCacheTagsAsync` удалён. Никто ещё не сужает — поведение не меняется | ✓ 22 теста `CacheRowTagsTests` (§4-1); мутации «верить исходным значениям всегда», «без `Watch`», «не пропускать временный ключ», «без старого значения FK», «слияние транзакции без объединения» — каждая роняет тесты; ✓ все 1861 зелёные; ✓ вживую: правка расписания группы 24 → метки «table:HubGroups, row:HubGroups:24, row:Clubs:438», выкинуто 4 (как до этапа) |
| ✅ К4б.3 | **Сужение при чтении** (§2.3): `db.CacheRows<TRoot>(ids, tables…)` (`CacheRowsExtensions`, проверка по модели: корень из реестра, прямой FK) → блок `CacheBuildScope.Narrow`, привязанный к своей сборке; перехватчик команд — `scope.TouchTable`; потолок 200 id; вложенный блок заменяет внешний; выключатель `CacheRowPrecision` (§2.5, дефолт выкл.); **сверка на попадании** (§4-6, `CacheHitVerifyPercent`); страж мест сужения (§4-4, `CacheRowsSitesTests`); подметание `_tags` (§7). Мест сужения ещё нет — поведение не меняется | ✓ 18 тестов `CacheRowNarrowingTests` (в т.ч. настоящий SQL на живом PG, только чтение), 7 — `CacheHitVerificationTests`, 2 — страж; мутации «вложенная сборка наследует сужение», «сверки нет» (сверка ловит нарочно сломанное сужение), «выключатель не действует», «нет потолка», «сверка читает вложенные из кэша», «сброс во время сверки — подозрение», «подметание без отмены», «перехватчик мимо сужения» — каждая роняет тесты; ✓ все 1889 зелёные; ✓ вживую на :5079: настройки с дефолтами Development (20 %), выключатель вкл./выкл. — страницы 200, записей с `row:` нет, блок сверки «сверено 0» |
| ✅ К4б.4 | **Группа** (§3.1): загрузка по id (со сверкой slug), блоки Q1/Q2/Q3a/Q13, деление Q3 и Q14 (Q14 — двумя запросами) | ✓ 15 тестов `CacheGroupPageNarrowingTests` на живом PG (метки настоящей страницы, выключатель, сверка без расхождений, 12 сценариев) + 4 места в страже; мутации «официальная группа клуба сужена блоком группы», «медиа сужены не своими id» (недосброс) и «медиа без блока» (потеря точности) — каждая роняет свой сценарий; ✓ все 1904 зелёные; ✓ старые запросы Q3/Q14 и новые на живой базе дают тот же JSON (временный тест); ✓ вживую на :5079 (выключатель вкл.): правка расписания группы 24 → «SaveChanges: HubGroups ~1 (TrainingSchedule, UpdatedAt)», метки `table:HubGroups, row:HubGroups:24, row:Clubs:438`, выкинуто 4 — страница 24, список, обзоры клубов 438 и 482; **страница группы 17 жива** (до К4б.4 падала). Обзор чужого клуба падает — он читает группы таблицей до К4б.5 (в первой редакции строки ожидание «жив» было преждевременным). Прокликивание — сверено 14, расхождений 0 |
| К4б.5 | **Клуб** (§3.2): в обзоре — официальная группа, состав — целиком | правка группы без клуба не роняет обзоры; группы клуба 438 — только обзор 438; правка пловца клуба X — только состав X (и табличные читатели `Swimmers`); ноль подозрений сверки |
| К4б.6 | **Служебные колонки** (§2.6): реестр по журналу, `col:` при записи и чтении, явный метод для массовой служебной записи (стирание объединённых мест), выключатель `cache.columnPrecision` | тесты §4-1/§4-2 для колонок; сценарий «loglig-правка — season-best, обзор клуба и страница группы живы»; вживую: прогон проверки качества не выкидывает витрины, которые `QualityScannedAt` не читают; ноль подозрений сверки |
| К4б.7 | **Пловец — по замеру журнала на проде** (§3.3), только если замер покажет нужду: swims (эстафеты через `Relays`), profile, career-id, предстоящие старты | правка чужого пловца не роняет профиль; правка времени эстафеты роняет профили всех её этапов |

К4б.0 и К4б.1 полезны сами по себе и ничем не рискуют — можно отдельным маленьким PR сразу после
К4. Риск сосредоточен в К4б.4–К4б.6: по одному шагу, у каждого своя приёмка со сверкой. Каждый
этап — со своими доками (§10); выключатели включаются по умолчанию после приёмки своего этапа.

## 6. Чего план не лечит

- **Удаление медиа** роняет страницы всех групп: каскад в публикации (§2.2-5). Удаляют реже, чем
  добавляют; если журнал покажет иное — `UserMediaRepository.RemoveAsync` предзагружает
  публикации и сбрасывает метки их групп явно.
- **Не-служебные правки общих таблиц** (`Swimmers`, `Clubs`, `Results` админом, новая страна от
  владельца группы) роняют всех табличных читателей, как сейчас. Это редкие записи.
- **Команды `dotnet run -- --флаг`** — отдельный процесс со своим кэшем, до веб-сервера их сброс не
  доходит (как и сейчас). Лечит Redis (К6).
- **Кэш браузера**: у страниц пловца `max-age=60` — правку увидят через минуту, как бы точно ни
  сбросился сервер.

## 7. Redis (К6) и память — что добавить

- У записи может быть сотня меток строк → чтение сверяет их версии одним `MGET`; потолок M (§2.3)
  держит его коротким. Меток `col:` — единицы (реестр).
- Меток строк много, ключам их версий нужен TTL. ⚠ Версия не должна начинаться заново: протух
  ключ, создался снова с «1» — и старая запись с версией «1» совпала бы. Версия — уникальное
  значение (не счётчик с нуля), либо TTL ключа версии заведомо больше самого долгого TTL записи
  (сутки).
- Память сейчас: словарь `_tags` в `MemoryCacheService` копит источники меток строк до общего
  сброса. Подметание: при превышении порога ОТМЕНИТЬ и выбросить источники, на которые не ссылается
  ни одна живая запись. Отмена безопасна (лишний сброс сборки в полёте); выброс без отмены — нет:
  сборка с этим токеном потом не узнала бы о сбросе. Сделано в К4б.3
  (`MemoryCacheService.SweepTags`): порог 20 000 источников, размер смотрится раз в 1024 касания
  (`Count` у `ConcurrentDictionary` берёт все блокировки).
- Сверка на попадании (§4-6) и журнал (§4-5) — диагностика одного экземпляра; с Redis остаются
  такими же, у каждого экземпляра свои.

## 8. Решения Влада (14.09.2026)

1. **Пловец** — по замеру журнала на проде (§3.3, К4б.7).
2. **Выключатели** — да (§2.5); сделаны двумя, строки и колонки отдельно.
3. **Служебные колонки** — да, отдельным этапом после строки (§2.6, К4б.6). Узкое исключение из
   «по колонкам не дробим» (§3.3 cache-tags-plan): только реестр, читатель — по тексту SQL.
4. **Сверка на попадании** — да, обязательна в Development (§4-6), на проде выключена.

Открытых вопросов нет.

## 9. Попутные находки (14.09.2026)

- **Публичные ленты публикаций медиа** (клуба и группы) переведены на отдельную проекцию для
  зрителя вместо строки inbox-а модерации — сделано в К4б.0 (docs/media-page.md, «Лента
  зрителя — без владельца медиа»).
- `SwimmersPublicController.BuildProfileAsync` мутировал объект `swimmer-profile:{id}` из кэша
  (нарушение правила §4-3 cache-tags-plan) → исправлено в К4б.0 (`SwimmerProfileDto.CopyForPage`).
- Ключ обзора клуба не содержит `RequestedId`, а ответ содержит: склеенный клуб и канонический
  делят одну запись, `requested_id` — того, чей запрос собрал её первым. Клиент поле не читает —
  не чиним, помним.
- Импорт: чистка пловцов-сирот (`JsonImportService` ~897-907, 1892-1902) не проверяет
  `CompetitionEntries` (Restrict FK) и может откатить импорт — тот же класс, что «merge пловцов без
  CompetitionEntries» в задаче про пробелы админских путей записи.

## 10. Как вернуться к работе

### Состояние на 14.09.2026 (конец сессии К4б.4)

| Ветка | PR | Что внутри | CI |
|---|---|---|---|
| `cache-k4` (от master) | [#28](https://github.com/vladimirjpm/swimm-project/pull/28) → `master` | `207bd64` К4 (сброс по меткам сам) + `ad0a2bb` этот план | ✓ 3/3 |
| `cache-k4b-01` (от `cache-k4`) | [#29](https://github.com/vladimirjpm/swimm-project/pull/29) → `cache-k4` | `f21bcce` К4б.0 + К4б.1 | ✓ 3/3 |
| `cache-k4b-02` (от `cache-k4b-01`) | [#30](https://github.com/vladimirjpm/swimm-project/pull/30) → `cache-k4b-01` | `a412744` К4б.2 (метки строк при записи) + передача §10 после К4б.1 | ✓ 3/3 |
| `cache-k4b-03` (от `cache-k4b-02`) | [#31](https://github.com/vladimirjpm/swimm-project/pull/31) → `cache-k4b-02` | `8825ab6` К4б.3 (сужение при чтении, сверка, страж, подметание) | ✓ 3/3 |
| `cache-k4b-04` (от `cache-k4b-03`) | [#32](https://github.com/vladimirjpm/swimm-project/pull/32) → `cache-k4b-03` | К4б.4 (страница группы на блоках сужения) + эта передача | см. PR |

- **Вливать по порядку: #28 → #29 → #30 → #31 → #32.** После слияния #28 GitHub сам
  перенацелит #29 на `master` (и так далее по цепочке). Миграций нет — на проде применять нечего.
- Автоисправление (CI, конфликты, ревью) включали на #28–#31 в прошлых сессиях, на #32 — в сессии
  К4б.4. В новом чате его при желании включают заново для своего PR.
- Тесты 1904/1904 (после К4б.3 было 1889: +15 тестов К4б.4).
- **Ждёт решения Влада:** дефолт выключателя `CacheRowPrecision` после приёмки К4б.4 (§5: «включаются
  по умолчанию после приёмки своего этапа»). Сейчас дефолт `false` — сужение на проде не работает,
  пока его не включат на `/Admin/Settings` (живёт в памяти — рестарт вернёт дефолт). Варианты:
  включить дефолтом сейчас (выгода — страницы групп; риск — только сужение группы, сценарии и
  сверка чистые) или ждать К4б.5 и включать сразу группу с клубом. Сверка на проде выключена
  (`CacheHitVerifyPercent` 0), так что недосброс на проде увидели бы только пользователи.

### Что сделано в К4б.2 (чтобы не искать)

- `CacheTags.AnyRow(table)`. Строковая перегрузка `Row` не понадобилась: у всех корней ключ `int`,
  а `Countries` (ключ тоже `int`) не корень — прежняя пометка про alpha-3 была неверна.
- `Swimm.Infrastructure/Data/CacheRowRoots.cs` — реестр корней по типам сущностей;
  `CacheRowRoots.IsRoot(entityType)` пригодится К4б.3 для проверки блока сужения по модели.
- `CacheInvalidationInterceptor`: `Gather` собирает метки в `ChangeTags` (по таблице — метки строк
  или «неизвестно»); `RowLinks` — FK на корни по типу сущности, считается раз на тип; `Watch`
  (зовёт конструктор `SwimmDbContext`) — какие строки подключены не запросом (§2.2 п. 7).
  Метки транзакции сливаются в тот же `ChangeTags`, сжатие считается по сумме строк.
- `DbContextCacheExtensions.InvalidateTableCacheAsync<T>()` — единственный явный сброс; `TableTag`
  и `InvalidateCacheTagsAsync` удалены, три места переведены.
- Шов для тестов: `PreviewTags(db)` — метки текущих изменений без сохранения (временный ключ
  виден только до сохранения и только у Npgsql: InMemory раздаёт постоянные id сразу при `Add`).

### Что сделано в К4б.3 (чтобы не искать)

- `Swimm.Infrastructure/Data/CacheRowsExtensions.cs` — `db.CacheRows<TRoot>(id | ids, typeof(…)…)`:
  проверка по модели (кэшируется на модель), вне сборки — пустой блок.
- `CacheBuildScope`: `Narrow(root, ids, tables)` — блок в своей `AsyncLocal`, привязан к сборке,
  в которой открыт; `TouchTable(name)` — `table:T` или, в блоке своей сборки, `row:` + `anyrow:`;
  `RowPrecision` (выключатель на момент начала сборки); `BeginVerification()` — сборка-сверка.
  `CacheDependencyInterceptor` зовёт `TouchTable`.
- `MemoryCacheService`: берёт `ISettingsService` (необязательный параметр конструктора — тесты
  без настроек живут как раньше); сверка `MatchesFreshAsync` на попадании в запись с
  `row:`/`anyrow:`; журнал — `CacheJournal.HitChecks`; подметание `SweepTags` (порог
  `TagSweepThreshold`, для тестов — `init`).
- Настройки — `CacheSettings` (`CacheRowPrecision`, `CacheHitVerifyPercent`); дефолт доли по среде:
  `AdminSettingsService(cache, development)`, регистрация в DI фабрикой с `IHostEnvironment`.
- Страж — `CacheRowsSitesTests` (`Covered`), общий сканер IL — `Swimm.Tests/IlCalls.cs`.
- Тестовые настройки кэша — `CacheSettingsStub` (в `CacheRowNarrowingTests.cs`).

### Что сделано в К4б.4 (чтобы не искать)

- `HubGroupPublicRepository.GetPageAsync(groupId, slug)` (бывший `GetBySlugAsync`; интерфейс
  `IHubGroupPublicRepository`): блок группы — строка группы (`Id AND Slug`), участники, подписка;
  отдельный блок клуба — официальная группа клуба подписки. Подписка выходит из блока проекцией
  `FollowedClubRow`. Контроллер (`HubGroupsController.GetGroup`) зовёт с `access.Id`.
- `HubGroupMediaService.GetGalleryAsync` — блок группы по `Sys_HubGroupMedia`.
- `UserMediaPublicationService`: `GetApprovedForGroupAsync` — блок группы по публикациям;
  `PublishedItemsAsync` (общий для ленты группы и клуба, бывший `QueryPublishedItems`) — публикации,
  потом медиа по списку id в блоке `CacheRows<UserMedia>` (проекция `PublishedMediaRow`).
- Тесты — `CacheGroupPageNarrowingTests`: класс `World` (живая база → страницы всех групп и
  список в кэше с выключателем; `Pair()` — две группы, не связанные клубом; `Mirror()` — InMemory с
  теми же id групп и медиа, перехватчик сбрасывает в тот же кэш). Для клуба (К4б.5) его можно
  расширить обзорами и составами, не заводя второй.

### С чего начать К4б.5 — клуб (§3.2)

- **Сначала — решение Влада о дефолте выключателя** (вверху §10). От него зависит, включать ли
  дефолт в том же PR.
- **Ветка:** если цепочка #28 → … → #32 уже в master — от `master`; иначе от `cache-k4b-04`
  (PR снова «поверх», база `cache-k4b-04`).
- **Что меняется** (разбор — §3.2; строки на 14.09.2026, сверить с кодом):
  - Обзор — `ClubOverviewRepository` ~116-117: `HubGroups WHERE ClubId = rid AND IsOfficial` →
    блок `CacheRows<Club>(rid, typeof(HubGroup))`. Других чтений `HubGroups` в обзоре нет —
    проверить. Id клуба известен до сборки (`ResolveClubIdAsync` снаружи, склеенный → канонический).
  - Состав — `ClubPublicRepository.GetRosterAsync` (~55-165): `Swimmers WHERE ClubId` (~68) и
    `Results WHERE ClubId AND SwimmerId IN` (~121) → блок `CacheRows<Club>(rid, typeof(Swimmer),
    typeof(ResultRecord))`. ⚠ Правило: КАЖДЫЙ запрос состава, читающий `Swimmers` или `Results`,
    должен фильтровать по `ClubId` — пройти метод целиком (счётчики, сортировки, «последний старт»).
  - Season-best клуба и стена рекордов — не трогать (§3.2).
  - Места — в `CacheRowsSitesTests.Covered`, сценарии — по §5 К4б.5: правка группы без клуба не
    роняет обзоры; правка группы клуба 438 — только обзор 438; правка пловца клуба X — только
    состав X (и табличные читатели `Swimmers`); переход пловца между клубами — оба состава.
- **Приёмка вживую:** как в К4б.4 (строка §5) — с выключателем вкл. правка расписания группы 24
  выкидывает обзор 438, но не обзор 482; ноль подозрений сверки.

### Грабли этой сессии

- **Репозиторий ПУБЛИЧНЫЙ.** Уязвимости в доках, коммитах и описаниях PR не расписывать — только
  нейтрально («ленты зрителя получили свою проекцию»). Коммит плана из-за этого переписывали.
- **PG-тесты молча пропускаются**, если базы нет (`if (db == null) return;`). Новое ключевое
  утверждение проверять мутацией: перевернуть → убедиться, что падает → вернуть.
- **Помощник `api()` на админ-страницах**: для PUT/POST с JSON нужен
  `headers: { 'Content-Type': 'application/json' }`, иначе 415. Пустой ответ 200 он принимает за
  ошибку JSON («Unexpected end of JSON input») — запрос при этом прошёл.
- **Правка данных для приёмки** — только обратимая и на тестовой группе 24: снимок до
  (`docker exec swimm-postgres psql … -At -c 'SELECT "TrainingSchedule"::text …'`), после — сверка
  `= '<снимок>'::jsonb`. Запись через :5079 не сбрасывает кэш API Влада на :5078 (другой процесс).
  Правка группы 24 для приёмки: `PUT /api/me/hub-groups/24/training-schedule` (тело —
  `{ place, slots: [{ day, start, end }] }`) через `api()` со страницы `/Admin/Cache`; возврат тем
  же запросом с прежним телом даёт пустой сброс (записи уже выкинуты первой правкой).
- **psql из PowerShell:** кавычки идентификаторов (`"HubGroups"`) PowerShell съедает по дороге в
  `docker exec` — SQL с такими именами гонять через Bash.
- **Трекер EF:** `ChangeTracker.Clear()` событий не шлёт; InMemory выдаёт постоянные id уже при
  `Add` (временных у него нет) — тесты временных ключей только на Npgsql.
- **Смена настройки на :5079** (`PUT /api/admin/settings/{key}`, тело `{ value }`, через `api()`)
  пишет строку аудита `setting.update` в локальную базу и сбрасывает весь кэш этого процесса.
- **Счётчик дефолтов настроек:** `AdminSettingsServiceTests.GetAll_OnInit_ContainsAllDefaults`
  считает их штучно — новая настройка правит число там же.
- **Сверка и «ложное подозрение» от времени (К4б.4):** у страницы группы `next_training`
  считается от текущего часа. Попадание в пределах TTL (5 мин) после начала занятия соберёт
  другое «ближайшее занятие» — сверка запишет расхождение, хотя сброс ни при чём. Смотреть, какое
  поле разошлось: `next_training` — не недосброс (запись и так выкинута и пересобрана).
- **«Старое против нового» без старого сервера (К4б.4):** :5078 может быть выключен. Равенство
  ответа проверяли временным тестом: старые запросы (из `git show cache-k4b-03:…`) и новые на
  живой базе → сравнение JSON → тест удалён. Дешевле, чем поднимать второй API из worktree.
- **InMemory-зеркало в сценариях:** сеять отдельным контекстом БЕЗ перехватчика (иначе сев сам
  сбросит страницы); новые строки InMemory получают id больше засеянных — с id живой базы у
  зеркала они не совпадут. Строки менять только загруженные запросом — подключённая не запросом
  даёт `anyrow:` (§2.2 п. 7), и сценарий «не роняет» ложно покраснеет.
- **Первая навигация pane** на `/Admin/Cache` сразу после `preview_start` бывает «denied or
  failed» — повторить `navigate`.

### Код и проверка

- Код: `Swimm.Application/Constants/CacheTags.cs` (метки),
  `Swimm.Infrastructure/Data/CacheRowRoots.cs` (корни сужения),
  `Swimm.Infrastructure/Data/CacheRowsExtensions.cs` (блок сужения),
  `Swimm.Infrastructure/Services/CacheBuildScope.cs` (сборка, сужение, сверка),
  `Swimm.Infrastructure/Services/CacheSettings.cs` (ключи выключателя и доли сверки),
  `Swimm.Infrastructure/Data/CacheDependencyInterceptor.cs` (чтение → метки),
  `Swimm.Infrastructure/Data/CacheInvalidationInterceptor.cs` (запись → метки, в т.ч. строк),
  `Swimm.Infrastructure/Services/MemoryCacheService.cs` (токены меток, снимок для `/Admin/Cache`).
  Места сужения — `CacheRowsSitesTests.Covered` (с К4б.4: страница группы).
- Проверка вживую: API Влада на :5078 не глушить. `dotnet build server/Swimm.API -c Release` →
  preview `swimm-api-5079` → прогреть `/api/hub-groups/{slug}`, `/api/clubs/{id}/overview`,
  `/api/swimmers/{id}` → `/Admin/Cache`, раскрыть «Все записи» (метки), с К4б.1 — журнал сбросов.
  Локально групп две: 17 (`dolphin-netanya-masters`, без клуба) и 24
  (`hapoel-dolphine-netanya-test`, клуб 438).
- Доки на каждом этапе (docs/pre-push-rules.md): тексты `/Admin/Cache` +
  `docs/admin-pages/cache.md`; выключатели и доля сверки → `docs/admin-pages/settings.md`; по
  завершении — §3.1/§3.3/§6 cache-tags-plan, ARCHITECTURE §5, `docs/important.md`.
