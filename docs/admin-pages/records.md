# /Admin/Records — рекорды и нормативы

Файлы: `Pages/Admin/Records/Index.cshtml`; API: `/api/admin/records`,
`/api/admin/normative-standards` (`RecordsAdminController`), diff/apply —
`RecordDiffService`, сиды — `RecordsSeeder`.

Три вкладки: рекорды (ISR Masters и др., импорт с diff-подтверждением),
нормативные стандарты (система разрядов; ось Country — alpha-3, RUS/ISR,
"" = универсальный набор). Данные питают normative-попап и карточки рекордов
на публичном сайте.

## Колонка «Holder (latin)» (2026-09-17)

Пишет `Records.HolderNameEn` — держателя латиницей для международных экранов
(`/records`, `/records/compare`), где ивритская строка посреди рейтинга двух сотен
стран читается как сбой кодировки (решение Влада, исключение в
[important.md](../important.md)). На остальной витрине имя по-прежнему ивритское.

Обычно поле заполняется САМО: его приносит источник, который отдаёт латиницу (World
Aquatics). Руками вписывают там, где источник латиницу не дал, — держателя нет ни в
одном нашем протоколе, и взять имя больше неоткуда (таких строк 13 из 90 по Израилю;
почему именно они — [data-integrity.md](../data-integrity.md), «Почему 13 израильских
рекордов остаются ивритом»).

Два правила поля, оба неочевидные:
- **пустая клетка НЕ стирает** сохранённое имя. Форма уходит целиком, и пустое поле
  означало бы «стереть» при каждой правке времени;
- **смена времени СБРАСЫВАЕТ** имя, если новое не вписали. Время — это тождество
  рекорда: сменилось — значит рекорд другой, и английское имя прежнего держателя к
  нему не относится. Правило одно с импортом — `HolderLatinName.CarryOver`.

## Качество справочника (2026-08-01)

Ошибки самих официальных файлов **не чиним** — наша копия обязана совпадать с
источником. Вместо правки — реестр `Sys_RecordIssues` (сущность `RecordIssue`) и
сверка с нашими протоколами `Sys_RecordVerifications` (`RecordQualityService`).
Вкладка **«Спорные записи»** (`?tab=issues`, она же цель дип-линка `?filter=issues`
с дашборда): фильтр по статусу, создание записи, правка причины/статуса/обоснования,
удаление. API — `/api/admin/record-issues` (GET/POST/PATCH/DELETE). В таблице рекордов
у каждой строки кнопка **⚑** — заводит претензию на эту ось с текущим временем; сам
рекорд при этом НЕ меняется. Если рекорд с тех пор сменился, в реестре у записи стоит
пометка «рекорд с тех пор сменился» (ключ реестра включает время).
Метрики и кнопка «Сверить с протоколами» — в блоке «Рекорды» на `/Admin`.

⚠ `Found = false` — не признак ошибки: у нас загружены протоколы не за все годы.
Полный разбор и реестр (первая запись RQ-1) — `docs/plans/records-quality-plan.md`.

## Кандидаты сторожа импорта (2026-09-15)

Статус **`candidate`** заводит не человек, а импорт рекордов (`RecordDiffService`, правила —
`RecordPlausibility`): неправдоподобное время из источника. Три правила:
`implausible-improvement` — мировой рекорд улучшен за раз больше чем на 3 %;
`faster-than-world-record` — рекорд страны / возраста / мастерса быстрее мирового той же
дисциплины; `slower-than-stored` — строка стала медленнее той, что уже лежит в базе.
Живые поводы — «мировой» 40.11 на 100 в/с ж 50 м в отчёте World Aquatics
(`docs/data-integrity.md` И-20) и два мастерских рекорда, потерянных федерацией (И-21).

⚠ У `slower-than-stored` нарочно нет порога и есть исключение: строки `country/*/open`
оно пропускает — там пишут два источника, и откат в середине `--records-refresh` штатный
(И-13). Поэтому «стала медленнее» на национальном рекорде в реестр НЕ попадёт.

- Сам рекорд записан **как в источнике** — не чиним. ⚠ ОДНО исключение с 17.09.2026 — ниже,
  «Защита оспоренного времени».
- На сайте метки **нет**, пока запись не переведена в `open` (публичные выборки берут только
  `open` / `reported` / `accepted`). Правило плана: автомат только предлагает, статус ставит
  человек. Ошибка сторожа — `rejected`.
- Кандидаты стоят в списке первыми; с дашборда — метрика «Кандидаты в спорные» →
  `?tab=issues&status=candidate`.
- Повторный импорт того же значения претензию не пересоздаёт и снятую (`rejected`) не
  воскрешает: ключ реестра — 8 осей + время.

## Защита оспоренного времени (2026-09-17)

**Статус претензии решает, возьмёт ли импорт это значение.** Если на время, приехавшее из
источника, есть запись в реестре со статусом `open` / `reported` / `accepted`, импорт его
**не берёт** — в базе остаётся прежнее (`RecordIssueStatuses.SourceOverruled`). Так защищаются
рекорды, которые источник ПОТЕРЯЛ (`docs/data-integrity.md` И-25). ⚠ Пока таких записей в
реестре нет — механизм профилактический.

- `candidate` **не защищает**: автомат только предлагает, решает человек. Чтобы защита
  включилась, претензию надо перевести в `open` — это и есть «я разобрался».
- `rejected` / `fixed-by-source` не защищают — там значение источника признано верным.
- Защита висит на ЗНАЧЕНИИ (ключ реестра = 8 осей + время). Федерация исправит файл → приедет
  другое время → защита снимется сама. Побитый по-настоящему рекорд она тоже не держит.
- В превью диффа на `/Admin/Import` защищённая строка **зачёркнута**, с пояснением «🛡 защищено
  реестром», и наверху стоит счётчик «Защищено реестром». После Apply то же число в тосте.

⚠ Претензия в `open` — не конец истории: справочник федерации от этого не исправится.
Доводи до `reported`, иначе расхождение с их сайтом будет жить вечно.

## Спорные записи: список карточек, а не таблица (2026-09-16)

Претензия — **карточка**: шапка (ось, время, причина, статус, кто и когда завёл, кнопки) и
обоснование широким полем под ней. Между карточками отступ, у каждой рамка и скругление.

**Почему не таблица.** Таблицей это было дважды и оба раза плохо. Одной строкой `textarea`
обоснования растягивала её так, что колонка с `Save`/`Del` уезжала за правый край и строка
выглядела без действий вовсе. Двумя строками разделить записи не вышло: ни чередование тона
(соседние пары читались как одна лента), ни зазор толстой границей — при `border-collapse` у
строк таблицы не работают ни отступы, ни рамка вокруг пары. У карточки всё это работает само,
и вся претензия лежит в одном элементе, поэтому `saveIssue` берёт все поля одним `rowField`.

Шапка карточки — **сетка с фиксированными колонками**: номер · ось · время · причина · статус · заведено · кнопки. Тянется только ось, остальные держат ширину — иначе поля вставали бы по своему содержимому и время одной записи оказывалось не под временем соседней (так и было при `flex-wrap`, первый подход к карточкам). Ширины подобраны под самые длинные значения: `faster-than-world-record` у причины, `fixed-by-source` у статуса.
Ниже 1180px сетка схлопывается в одну колонку — поля уезжают вниз, а не за край; подписи «Причина» и «Статус» остаются у полей, поэтому в столбик всё читается без шапки-заголовка.

⚠ **Кнопка `Save` обязательна:** выпадающие списки причины и статуса сами ничего не
сохраняют. `Save` отправляет карточку целиком — причину, статус и обоснование.

⚠ **`Del` не для «закрыть претензию».** Импорт не трогает запись, которая уже есть в реестре,
в ЛЮБОМ статусе — специально, чтобы отклонённое не воскресало. Удалишь — следующий прогон
того же файла заведёт кандидата заново.
