# План: фронт №2 после дедупа/автозабора (2026-07-16)

Хендофф для новой сессии. Предыдущий фронт (docs/tasks/next-front-plan.md) выполнен
целиком: дедуп A1–A4 + админ-UI 7.2, автозабор isr.org.il (фаза 6, PDF из loglig),
C1 многодневные на клиенте, хвосты Discovery (селектор событий, чистка пустых).
Всё закоммичено (…950bc22, 31bd208, 1a14188), 272/272 тестов. НЕ запушено — пушить
только по команде Влада.

## 0. Сначала — проверки Влада (блокируют часть работ)

- `/Admin/Swimmers`: решение по merge-парам (14 уверенных + спорные) и сиротам (192).
  Применение — из UI (dry-run → Применить). **До «да» Влада merge к БД не применять.**
- `/Admin/Discovery` вживую: sync → «Затянуть» → импорт (в т.ч. двухдневка через
  селектор событий); решить, включать ли `DiscoveryEnabled`.
- Глазами: секция Meets на competitions.html, счётчик competitions в карьере.

## Поток A. Фиксы по итогам ревью (мелкие, можно Сонету одним таском)

- A1. Discovery: статус «imported» ставить ПОСЛЕ успешного завершения import-job,
  а не при постановке в очередь (`DiscoveryAdminController.Import` — сейчас
  SetStatusAsync сразу после Enqueue). Вариант: писать discoveredId в job и метить
  из очереди по завершении; либо страница опрашивает статус job и метит с фронта.
- A2. `SwimmerMergeService`: защита от пересекающихся пар в одном вызове
  (один и тот же Id в двух парах, например A←B и C←B) → отклонять весь вызов
  с понятной ошибкой ДО каких-либо изменений. + юнит-тест.
- A3. Discovery `GET {id}/pdf`: убрать побочный эффект (RefreshDetails/запись
  LastError) из GET-пути — детали грузить только в POST preview/details.

## Поток B. Админка: UX-мелочи (Сонету через /delegate)

- B1. **PoolType — DDL вместо текстбокса** (просьба Влада 2026-07-16): в форме
  Admin/Competitions Edit/Create поле «Бассейн» сделать select с каноническими
  значениями (25m / 50m — взять фактические Distinct из БД + эти два дефолта;
  посмотреть, что уже использует Import: pdf-pool-type select в Import.cshtml —
  переиспользовать список). Валидация на сервере: непустое из допустимого набора
  (ValidateAsync в CompetitionAdminRepository). Проверить, что фильтры/уникальный
  индекс (Name, Date, PoolType) не ломаются.
- B2. (опционально, если Влад захочет) Кнопка массового удаления сирот на
  /Admin/Swimmers — строить ПОВЕРХ существующего списка Orphans; подтверждение
  как у merge. Не делать без явного «да».

## Поток C. Дальше по ROADMAP (по выбору Влада)

- Фаза 5 — публичные галереи соревнований.
- Фаза 7.1 и прочие удобства ввода в админке.
- Azure-деплой — только по явному запросу (решение принято, см. ROADMAP).
- B5-перспектива: loglig HTML (`LeagueTable/AthleticsDisciplineResults/<id>`) как
  источник ID пловцов — системный дедуп вместо матчинга по имени. Исследование.
- Поток D (низкий приоритет): пловцы эстафет + HE-masters-эстафеты Maccabiah;
  правки токенизации гонять на ВСЕХ эталонных PDF-тестах.

## Правила делегирования (как в прошлом фронте)

Сонету — A1–A3 и B1 (механика с ясной спецификацией; в промпт ОБЯЗАТЕЛЬНО
«не запускай других агентов, делай сам»; таск-доки docs/tasks/*-sonnet.md,
самодостаточные, коммит отдельно). Fable сам — всё, что трогает auth/безопасность,
миграции с неочевидными последствиями, исследование loglig.

## Footguns (наследие)

- Build-lock :5078 (Stop-Process осиротевших Swimm.API); Release + :5079 при занятом.
- Два DbContext: EF-команды с `--context SwimmDbContext`.
- Синтетика 3М (`Note='SYNTH'`, `SwimmerOrgId LIKE 'SYNTH-%'`) — НЕ удалять,
  дедуп-запросы её исключают.
- isr.org.il/loglig — чужой прод: троттлинг в провайдере не обходить; тесты НЕ ходят
  в сеть (фикстуры в Swimm.Tests/Fixtures/Discovery).
- Merge пловцов необратим; применение только из UI после dry-run и «да» Влада.
- Tailwind админки: после правок классов в Pages/Admin — `npm run css:build`
  и коммит admin.min.css.
- DevAdminBypass работает только в Development — живые проверки админки без логина.
